# Gate-5 controlled soak — 24 h verdict (2026-09-17/18)

Candidate `699d7415de3d2337405d746e840a0e43d668b9db` (all-four CI green). Arm: detached
worktree at that SHA, byte-verified build, loopback-only headless server on a scratch
namespace, enforced mock agents (no provider/model/credential can run). Ran 2026-09-17
05:10:40 UTC → 2026-09-18 05:10:5x UTC. Analyzer of record: `tmp/soak-run/analyze-soak.mjs`
(pre-declared thresholds in `tmp/soak-run/SOAK-PLAN.md`); raw evidence `tmp/soak-run/logs/`

- `perf/` (non-durable per §1h D; this file is the durable record).

## Verdict: PASS with one classified deviation (14/15 checks; the 15th is 3 HTTP blips in 2 870 cycles)

| Check                              | Result                                                                                                     |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Unexpected exits / drains / fatals | **0 / 0 / 0**                                                                                              |
| RSS slope                          | **−2.03 %/h** (shrinking; baseline 360 MB, max tree 514 MB, ceiling 1.5 GB)                                |
| Event-loop p99-of-p99              | **4.22 ms** vs 25 ms budget (151 711 samples; max single 79 ms = documented boot window)                   |
| SQLite `quick_check`               | **7/7 ok** (initial + 6 post-restart)                                                                      |
| Restart discipline                 | **6/6** scheduled restarts: every drain exit 0, no SIGKILL escalation, re-paired each time; ~1.1 s re-boot |
| Process/port leaks                 | **0**                                                                                                      |
| Mock-agent guard env               | held at every check                                                                                        |
| Workload cycles                    | **2 870+/2 870 OK** (assertions), 0 cycle errors                                                           |
| Unexpected HTTP errors             | **3** (0.1 %) — the deviation, below                                                                       |

## The deviation, classified

Three individual HTTP requests errored over 24 h at 10:10:45Z, 22:10:55Z, 02:10:59Z
(~6 h spacing, seconds after hourly-summary samples, **not** correlated with restarts at
:30, no cycle failed its assertions, no follow-on effect). Flake-class transient blips with
no defect signature — but the pre-declared threshold was zero, so the analyzer honestly
reports `allPass: false` on that check. Disposition for the user: accept the classified
verdict, or extend the soak to re-measure. (The known ACP installer churn-loop — recorded
separately as an open defect — was bounded, never touched a measured budget, and is
orthogonal to these blips.)

## Standing items carried out of the soak

- ACP installer retry-loop defect (filed in V4_MERGE_GATES.md; fix pending).
- The 72 h candidate-observation window starts from this verdict (2026-09-18 05:10 UTC).
