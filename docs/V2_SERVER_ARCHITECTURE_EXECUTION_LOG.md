# V2 server architecture execution log

This log tracks implementation of [the production plan](V2_SERVER_ARCHITECTURE_PRODUCTION_PLAN.md). The plan remains the acceptance contract. An implemented item is not complete until its integration and platform evidence meets that contract.

## Baseline and decisions

- Starting revision: `36e1649f017aeccebc202bb9d5c9c6ab0f797709`, branch `poracode/v2`.
- Starting worktree: only the untracked production plan; no application changes.
- Initial delegation used Crossagents/OpenCode DeepSeek 4.1 Flash. Current user-selected delegation is **Crossagents z.ai GLM 5.3 Flash High**; earlier provider failures and retries remain historical evidence, not active routing.
- Preserve the shared host core, supervisor authority, native clients, HTTP/WebSocket contracts, and sole database owner.
- Preserve managed-desktop quit behavior. Standalone defaults to foreground with its compatible web client. Service installation remains explicit.
- Support server-owned SSH environments and device-owned tunnels as distinct modes; no implicit credential migration or connection-triggered remote upgrade.
- Performance budgets in plan section 4 are proposed acceptance targets, not measured claims. Capture the corrected-instrumentation baseline before A1/A2 behavioral changes.
- Research precedes dependent execution. Executors own disjoint paths. Independent testers and an integrated review follow implementation; verified findings reopen affected rows.

## Current assessment — 2026-09-22 UTC

**About 70% of the overall goal achieved** (planning range 65–75%): approximately 85% of architecture/implementation and 35% of required production qualification; final architecture commit/push remains outstanding. The weighted method, latest artifact identities, confirmed follow-ups and revised completion criteria are in [plan section 0](V2_SERVER_ARCHITECTURE_PRODUCTION_PLAN.md#0-current-goal-evidence-and-remaining-delivery-plan). This estimate does not waive a release gate or measure remaining elapsed time.

The latest frozen full Electron mock smoke, macOS standalone install/CLI and seven actual multi-host scenarios pass. The subsequent managed-history correction has focused proof but is not yet in those artifacts. Procedure-owner mismatches and uncertain-launch retention are confirmed open corrections. Quiet capacity/soak, current full cross-client journeys, Windows/WSL, other claimed targets, physical devices and published N−1 qualification remain open.

## Requirement ledger

| ID  | Required outcome                                         | Implementation state                                                                                                           | Required evidence / current gap                                                                             |
| --- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| A0  | Valid latency diagnostics and reproducible load baseline | Population/key harness corrected; confirmed managed terminal routing fix active; uncontaminated latency qualification open     | Standards-shaped observer tests; real trusted input and blocking control; frozen workload/hardware evidence |
| A1  | Managed visible-thread interests reach the socket        | Interest, recovery and capacity UI pass focused checks; real load gates open                                                   | Hidden bulk isolation, retained control notifications, selection recovery, independent windows              |
| A2  | Native-only backend-to-main projection                   | Projection and hydration candidate verified; real load/isolation gates open                                                    | Zero bulk bytes; native sleep/reset parity; stalled-main isolation                                          |
| A3  | Bounded per-host decode and recovery; fair drains        | Transport retention and projection/recovery corrections pass parent checks                                                     | Host A flood leaves B connected; worker crash without synchronous bulk fallback; ordered convergence        |
| A4  | Managed socket health/deadline/recovery policy           | Managed recovery candidate implemented; lifecycle/load proof open                                                              | Never-open/half-open, restart, token/port changes, suspend/resume, no duplicate watches                     |
| A5  | No blocking shell settings/file/SSH work                 | Async promotion/import implemented and measured; packaged main trace open                                                      | Cached settings; asynchronous I/O; main-loop traces during settings/image/SSH load                          |
| B1  | Bounded persistence and truthful failure contract        | Durable gap and HTTP/WS acknowledgement reviewed; actual client notice/recovery UI remains open                                | Producer flow control, committed-prefix barriers, storage faults, control responsiveness, joined shutdown   |
| B2  | Principal/digest-bound, crash-aware command receipts     | Crash-aware receipts implemented; uncertain launch identity loss after navigation reproduced and reopened (R2)                 | Pre/post-side-effect crashes, changed-body conflict, stable principal, native/wire/schema compatibility     |
| B3  | Principal and aggregate fairness                         | Protocol/transport corrections pass parent checks; packaged gates open                                                         | Many sockets/slow peers cannot consume another client's control capacity                                    |
| B4  | Bounded normal reads and compatible pagination           | Bounded host/SDK, managed and native adoption implemented; redundant managed goal read corrected; final artifact/UI gates open | 10,000-thread catalogs, long histories, reconnect storms, supported older clients                           |
| B5  | Async bounded subordinate browser cache                  | Real Chromium cache fixture passed; full PWA/paired-host/WebKit qualification remains open                                     | Existing-cache migration, offline drafts/selections, stale-cache rejection, responsiveness                  |
| B6  | Bounded native recovery buffers                          | Native lifecycle/deadline corrections complete; device UI qualification open                                                   | Delayed history, replacement semantics, generation/host changes, native profiling and common fixtures       |
| C1  | Shared server-owned SSH environment identity/API         | Shared environment runtime/API/proxy and client integration implemented; current all-client live qualification open            | ADR, stable IDs/permissions/custody, all clients use same environment, restart/migration                    |
| C2  | Ownership-safe SSH bootstrap                             | Lock corrections implemented; integrated packaged SSH gate open                                                                | Unrelated stale PID, concurrent connects, slow owner, incompatible runtime, interrupted install             |
| C3  | Off-main SSH staging and complete cancellation           | Exit-join corrections implemented; packaged/main-loop proof open                                                               | Cancel every stage, dispose/config races, immutable artifact closure, no late tunnel/orphan                 |
| C4  | Nonblocking WSL staging and preserved identity           | Install/retirement/marker-version corrections complete; real Windows/WSL gate open                                             | Actual Windows/WSL cold/restarted/stalled/concurrent/non-ASCII and networking matrix                        |
| D1  | Publishable versioned `npx poracode` entry               | Real packed CLI and exact macOS arm64 runtime qualify outside checkout; registry/other target gates open                       | Packed package from empty external cwd, clean cache, target/Node matrix, registry ownership before publish  |
| D2  | Self-contained compatible web client                     | Compatible production web bundled; real desktop/mobile-web journey running against build03                                     | Fresh install desktop/mobile pairing, refresh/assets/locales/cache/version skew                             |
| D3  | Qualify and promote exact immutable artifacts            | Build03 immutable macOS artifact/install bytes verified; other platform and final corrected artifact gates open                | Support matrix, frozen native closure, hashes/provenance, real platform jobs                                |
| D4  | Authenticated upgrade identity and data compatibility    | Current same-tarball upgrade and earlier crash recovery verified; published N−1 and target matrix remain open                  | Wrong owner/build, concurrent upgrades, N−1 migration/rollback and crash matrix                             |
| D5  | Shutdown deadline survives cleanup failure               | Lifecycle corrections tested; current standalone qualification stops owner successfully; final platform/watchdog gates open    | Retained-handle process test; custody retained; external watchdog test for blocked loop                     |
| E1  | Enforced Electron-free reusable dependency graph         | Boundary implementation and standalone build verified; generic remote procedure ownership audit reopened 12 entries (R1)       | Real shared-service moves, static/lazy/worker/manifest graph checks, independent server build               |
| E2  | Responsibility-based extraction during behavioral work   | Responsibility cuts implemented; latest history simplification removes redundant work; final touched-file review open          | Identify concrete responsibility cuts and preserve lifecycle/transaction/provider/UI semantics              |
| E3  | Proven deletion and actual duplication consolidation     | Proven relay/shim/attachment cleanup completed; stale procedure entries and remaining reachable/dead-path dispositions open    | Reachability/build/dynamic-consumer inventory and per-deletion evidence                                     |
| E4  | Current runnable install/service/operations docs         | Plan, completion criteria and progress refined; install/CLI evidence current; final runbooks/claims reconciliation open        | Commands checked against qualified bytes; truthful capabilities/platform gates; promotion localization      |

## Integration and final review gates

The original gate list remains mandatory, but is no longer all unrun. Full checks, codegen/localization and selected real PTY/lifecycle/install/multi-host gates have checkpoint evidence recorded below. Re-run only invalidated checks during correction, then establish one final candidate. Current gaps are summarized above and in plan section 0; unrun native/platform/load/N−1 gates remain open. A passing older checkpoint is never silently transferred to new source/artifact bytes.

Conditional changes (database worker, worker reducers, maintained query summaries) require measured justification. Declining an unjustified conditional change does not waive its underlying latency, durability, or boundedness gate.

## Execution evidence

### Initial dispatch

Four source-read-only research lanes cover clients, host authority, environments, and distribution/structure. One bounded executor repairs the Event Timing adapter and adjacent tests. Research reports and raw local evidence live under `tmp/v2-production/`; durable conclusions and actual verification results are recorded here after inspection.

No production-readiness or completion claim has been made.

### Architecture challenge review checkpoint

The review keeps the shared host/supervisor/HTTP-WebSocket architecture. The following are current source findings, not measured latency results, and remain open after the initial candidates:

- A2: `backend/index.ts` still composes `createSupervisorEventRelay`; the relay still emits bulk supervisor payloads to main after remote delivery. Native-only projection is not implemented yet.
- B3: `sendRaw` enforces the per-socket cap immediately, but `enforcePrincipalOutboundBudgets` runs on connection admission and the default 30-second heartbeat. The advertised 16 MiB principal / 64 MiB global queue limits are not admission-time bounds. Socket-count limits make the overall exposure finite; describing it as wholly unbounded would also be inaccurate.
- B4: shell/thread snapshot pagination remains opt-in; unpaginated branches still materialize the full catalog/history. Compatibility needs an explicit bounded policy and client rollout.
- B2: the native continuation reports true process-crash coverage and native uncertainty classification. Parent source inspection confirms the renderer still rolls optimistic working state back for `command_outcome_uncertain`; web/Electron parity remains open.
- A1: the 200-interest overflow callback is wired to `console.warn`, not a visible client capacity state. This is a lower-priority overload UX gap, not evidence of failure at ordinary pane counts.
- C1: environment registry code exists, but no production composition instantiates it yet. Shared environment routes, dual authorization, and all-client UI are still required.
- D1/D3: the checked-in npm runtime manifest has no targets; generated qualified artifacts and actual external-install/platform evidence remain required. The launcher scaffold is not proof that the public `npx` product is ready.
- E1: real host/backend dependencies on supervisor, app-controls, schedules, and browser helpers under `src/main` remain, alongside compatibility reexports. These categories must be distinguished before moving or deleting files.

The real A0 preflight log records failure before measurement (`renderer diagnostics did not activate after reload`). Its correction is running; no renderer responsiveness result is inferred. Previous V6 completion covers its stated checklist and does not establish the larger current production acceptance contract.

### Qualification research correction

The first qualification report incorrectly said `tests/native-e2e/sharedHostLoadProfile.test.ts` did not exist. Parent inspection confirmed that file and its real-host, client, process, and provenance helpers are present. Its header explicitly distinguishes protocol-client/PTY coverage from real rendered GUI behavior. The report is not accepted as an authoritative inventory; the tester is correcting it and extending the existing helpers for real-renderer qualification. Old temporary G4 measurements do not establish the new input-to-paint gate.

### Observer candidate and first correction

The first A0 executor reported 37 focused diagnostics tests passing, plus typecheck and touched lint/format. Parent inspected both diffs. Correct standards timestamps, explicit threshold, sample counts, and snapshot format 2 are retained. Before baseline, the correction lane must fix support detection when `observe()` silently ignores unsupported entry types, disconnect observers at disposal, extract cohesive observer logic from the expanded recorder, validate/report threshold semantics, and distinguish event samples from grouped interactions. Real rendered measurements remain open.

### Host research decisions

- Auth source confirms access-token refresh preserves the stored session ID, making it a viable grant principal across refresh/reconnect. Re-pairing creates a new grant.
- B2 must preserve legacy interrupted receipts as uncertain; the research proposal to delete them once during migration is rejected. A stored user-message row or admitted thread row does not prove provider acceptance. A second post-effect receipt state cannot eliminate the crash window. The executor must use genuine durable reconciliation proof or return explicit uncertainty.
- B1 requires further design review before execution. Rejected shortcuts include accepting an oversized event while claiming a hard bound, merely shifting backlog into supervisor memory, mixing host persistence counters with WebSocket sequence spaces, and describing a failed final flush as successful committed shutdown.
- Migration 47 is reserved for the receipt executor. Later schema work must coordinate rather than independently reuse that number.

### First parent verification

Parent ran `pnpm exec vitest run src/renderer/diagnostics/ src/server/cliRuntime.test.ts src/server/cliShutdownDeadline.test.ts`: **7 files, 61 tests passed**, 4.85 seconds. This verifies the corrected observer adapter/lifecycle and failed-disposal process deadline, not UI load or packaged shutdown.

The first native slice reports **34 focused and 1,333 full AppTests passing**, with a simulator Time Profiler trace through the actual follow-up queue receive path. Parent inspected the replacement-slot implementation and test/evidence paths. Remaining count-only native buffers, the unbounded Android agent-base transition buffer, and common semantic fixtures are assigned to a follow-up executor; B6 is not closed.

The reusable qualification harness writer terminated with incomplete output after creating its files. `get_status` confirmed that run was completed; a new bounded continuation is finishing and executing the existing candidate. There was no known live app handle to restart. Baseline must contain HEAD plus only the six corrected diagnostics source/test files, with other concurrent source changes excluded.

Parent independently read `.tmp/ios-app-tests-final.xcresult` using `xcresulttool`: **1,333 passed, 0 failed, 0 skipped**, iPhone 17 / iOS 26.5 simulator. This is the first B6 slice's test result, not the later native-bound changes or physical-device qualification.

Parent created detached `tmp/v2-production/arms/a0-baseline` at the starting revision, copied only the six corrected diagnostics files, and verified its status contains exactly those six paths. `tmp/v2-production/a0-observer-ack.json` records their SHA-256 hashes and sizes. Checkout-local `pnpm install --frozen-lockfile` passed; no shared `node_modules` directory symlink was used. Main-worktree A1/A4 implementation is now released while R0 remains preserved independently.

The second harness continuation also ended before reporting execution. It is terminal according to Crossagents; no benchmark success is inferred. A smaller assignment now verifies the existing helpers and supplies an executable single-cell command for parent integration testing.

### Browser cache correction

The initial B5 candidate's focused tests passed, but parent review found that an incremented transaction revision was mislabeled as compare-and-swap, delayed reads could overwrite newer in-memory writes/removals, legacy cleanup did not bind to the migrated payload, future-format deletion was insufficiently guarded, and pending callbacks/operations could grow behind stalled IndexedDB. These are assigned for correction along with the old app-store assertion and hot-path size accounting. B5 remains open.

### Environment and artifact decisions

- Packaging proceeds with a small npm launcher resolving a version-pinned immutable runtime through one shared overlay/install path, a frozen dependency closure, bundled web assets, and exact-hash promotion. Platform jobs that have not run remain unqualified.
- SSH bootstrap safety and WSL staging proceed independently. C1 needs an explicit two-authority HTTP/WS design: base-path support alone does not carry both parent and child authorization. The ADR must preserve each authority, credential refresh/revocation, and child identity pinning before implementation.

### Initial independent review and WSL continuation

Two independent reviewers covered behavior/proof and structure/public evidence for A0/D5. Parent retained the format-2 field vocabulary for the frozen comparison, accepted corrections to misleading comments, bounded shutdown diagnostics, test-helper duplication, and identity-safe repeated disposal. The correction executor must preserve the R0 worktree. These are incremental reviews; the required final integrated review remains open.

The first C4 slice reports 62 focused tests and 3,485 broader tests passing, plus a bundled staging-worker smoke. It explicitly leaves synchronous adapter/plugin-install waits and cold-home probes. Parent rejected those residual synchronous waits as satisfying C4 and assigned the complete asynchronous call chain and child cancellation. Real Windows/WSL evidence remains missing; the passing tests do not replace it.

Parent amended [the SSH environment ADR](ADR_SERVER_OWNED_ENVIRONMENTS.md) to distinguish authorized first provisioning from forbidden connection-triggered replacement, keep observed runtime state outside the durable registry, require trust confirmation before first provisioning, and require management plus operation plus forwarding scopes for environment lifecycle/trust mutations. A store-only executor is implementing identity/CAS/redaction without advertising an incomplete capability.

### Follow-up implementation and parent checks

- B3 immediate aggregate send admission, B2 renderer uncertainty, and independent B5/B6 review are assigned to separate scopes. An independent D1/D3 artifact review is also running.
- Parent identified the existing `acp-generic` instance configuration as the structured workload seam: explicit local executable, real supervisor-owned child, existing ACP normalization. A deterministic bounded child fixture is being built without modifying production launch guards or injecting renderer events.
- Parent ran the real receipt crash, canonical digest/wrapper, and v46-to-v47 migration suites: **3 files, 21 tests passed**. This is focused evidence; broader client/UI and integrated release gates remain open.
- D2 candidate reports **139 focused tests**, **744 remote tests**, and **28 static external-layout checks** passing. These are executor reports; the real browser/PWA and fresh packaged artifact gates remain open.
- Resource audit corrected the earlier Android availability claim: the SDK and emulators exist locally. Windows 11 WSL2 and physical-device access were requested from the user; local macOS/browser/simulator/container work continues independently.

### Lifecycle review reopened C3/C4

- C3 candidate moved device-local SSH orchestration out of main. Parent inspection found that utility `waitForExit` resolves immediately after a timeout kill without observing exit; worker disposal joins the manager but not separately admitted archive preparation. Startup early-rejection handling also needs a regression. A scoped correction owns utility supervisor/service/protocol files; C1 runtime owns the manager and registry separately. Packaging still needs the real utility resource and immutable archive integration.
- C4 continuation reports 5,300 supervisor tests passing and real local worker/child joins, but explicitly leaves synchronous cold WSL shell/home probes, verification reads, Gemini launch writes, and Codex skill reads. These remain within the required shared-supervisor responsiveness outcome; a follow-up owns the complete cold paths. Real Windows/WSL proof remains open.
- The initial C1 runtime assignment ended with no output; `get_status` confirmed it terminal, and no runtime report or new service exists. A bounded registry correction is assigned first; runtime/API/proxy/client work remains required. The ADR was corrected to expose configured target ports while keeping local tunnel endpoints private. Parent also identified synchronous registry persistence and async temporary-file ownership cleanup for correction in this lane.
- D2 parent review reopened static I/O and stream lifetime: replace per-request synchronous filesystem work, join admitted streaming until finish/abort, and explicitly close streams on client disconnect.
- E1/E3 dependency/reachability tooling is assigned before moves/deletions, including lazy imports, worker/build entries and unresolved dynamic consumers. No permanent violation allowlist is accepted as boundary completion.

### B1 candidate and dependent work

B1 reports 370 focused tests passing, with bounded queue/controller and supervisor flow control candidates. Parent has not accepted its claimed end-to-end completion: the plan explicitly requires asynchronous committed-prefix read barriers, while the report describes synchronous barriers, and canonical refusal/stop handling needs independent scrutiny. An independent persistence reviewer is checking these semantics before B4 builds on them. A2 is now assigned to remove the redundant bulk relay while preserving B1 wiring and current WS interest subscriptions.

### Independent follow-up checks

- Parent re-ran D2 static routing/HTTP/helper suites: **4 files, 34 tests passed**. Review still requires response lifetime to cover writable completion and owned read cleanup; a focused follow-up is verifying the read-close versus response-finish gap.
- B3 immediate reservation implementation reports 88 focused tests passing, but parent reproduced an early-release violation with real `ws`: buffered bytes were **2,097,172 before terminate and immediately after terminate**, with zero send callbacks; only the actual close reduced the queue to zero. The implementation released accounting before termination/close. A correction now retains accounting until transport release and accounts for framing overhead. Raw probe: `tmp/v2-production/b3-terminate-probe.json`.
- Independent B5/B6 review found two probe-confirmed cache removal bugs and native accounting/deadline/tape gaps. B5 correction preserves future formats and documented subordinate-cache cross-tab policy. B6 correction must enforce quiet-stream expiry and real incremental tape parity; merely renaming age to arrival span does not satisfy the plan.

### Artifact review blockers

Independent review reproduced six release blockers: undeclared first-party installer input breaks the server build; the new workspace package lacks a frozen-lockfile importer; launcher qualification deletes the supplied real tarball before upload; npm reify removes the pre-applied Linux node-pty overlay; generated qualified manifests are not consumed by an npm publication pipeline; and the Rust action SHA does not resolve. Parent inspected the deleting call and installer order. These findings supersede the initial artifact candidate's completion language. A consolidated artifact correction includes cache-lock ownership, utility packaging, and exact-hash target aggregation. Mac standalone remains in the accepted qualification scope; a reviewer interpretation allowing Linux-only does not narrow it.

### A3 and D4 review follow-ups

A3's initial candidate reports 1,095 related tests passing and moves managed decoding off the renderer. Parent inspection found that posted raw work is removed from byte accounting while still retained, and decoded results waiting for an earlier sequence are not included in the drain's pending bounds. A scoped executor is reproducing/correcting total retained accounting and reviewing large auxiliary JSON fallback. The real rendered load gate remains open.

D4 reports 173 focused and 670 related tests passing; authenticated status/admit, staging, migration classification, journal and lock are implemented candidates. An independent reviewer is checking actual crash/rollback/ownership semantics. The N−1 artifact integration is still skipped without a fresh D4-capable artifact, so D4 remains open.

### Persistence candidate rejected pending correction

Independent scratch repros confirmed: refused canonical events still publish and later snapshots can silently omit them behind a fresh cursor; deferred reset classifies its typed degraded barrier as fatal; canonical-overflow stopping emits control traffic into the saturated sender and can kill the whole supervisor; per-thread refusal escalates the whole host; and the watermark leaves less headroom than the supervisor's in-flight queue. Existing focused tests missed these cases. These are correctness blockers, not optional performance polish.

A bounded corrective design is assigned before execution, including asynchronous committed-prefix barriers, a complete sync-reader caller inventory, admission/publication consistency, bounded in-flight flow, and reserved control capacity. Parent explicitly rejects reclassifying the plan's asynchronous barrier as optional. B4 remains pending this correction.

### Further adversarial review: startup, upgrades, and retained work

The architectural verdict remains: keep the shared host/supervisor and client contracts; finish their ownership and failure guarantees. These findings concern the checked-out baseline and current hardening candidates as identified below. They do not establish a measured throughput or latency result.

- **Baseline startup:** an independent source trace and focused reproduction establish that project-less `dbGetThreadsPage({ limit: 100 })` hydration resolves locally while its remote-only classification makes preload refuse it. Rejected storage hydration leaves `hasHydrated()` false. This is present in frozen R0 and the current candidate; fixture row IDs and timing are not the cause. Raw preload host read-back bypasses the failing renderer route and therefore does not prove normal startup. A2 owns the narrow routing correction and compatibility audit. A0 must assert natural hydration before installing workload fixtures. Frozen R0 remains untouched; any minimal boot-corrected comparison arm must be separately named and hashed, with its deviation disclosed. The main-local passthrough procedure overlap also needs a real dispatcher/ownership audit rather than assuming every advertised procedure has an implementation.
- **D4 candidate:** independent review reproduced fail-open staging when the upgrade journal is unreadable, corrupt, or from a future format; unknown post-failure schema can still permit unsafe old-code rollback; interrupted upgrades lack an operational recovery path. The review also found that the purported N−1 proof repacks current code and changes a schema number, and CI can pass when the artifact test skips. Authenticated owner/build qualification, lease takeover, and backup consistency held up in that review. A correction executor owns the concrete admission/recovery defects and truthful artifact tests; genuine previous-release qualification remains open.
- **B2 renderer candidate:** parent source inspection confirms only explicit `409 command_outcome_uncertain` preserves uncertain state. A timeout from the TS transport after dispatch still reaches ordinary optimistic rollback/failure handling, unlike the existing native ambiguity classification. A scoped executor must reproduce lost response after effect, preserve command identity, distinguish pre-send failures, and audit the managed start route's receipt coverage.
- **A3 correction candidate:** parent ran three engine/drain suites, **36 tests passed**. A further parent reproduction nevertheless found that repeated renewals release accounting without releasing a live worker's queued clones: 100 posted 64-byte frames leave **0 accounted bytes and 6,400 retained modeled worker bytes**, without worker termination. Posting an acknowledgement does not synchronously reclaim messages ahead of it. The reproduction uses a paused-worker transport model, not a real-browser memory measurement. A second correction must retain transport credits until actual response or termination across timeout, renewal, disposal, and reset. Auxiliary stringify inputs also remain unbounded graphs despite count limits and a nominal reservation; that residual cannot support a hard byte-bound claim.
- **B3 protocol frames:** the active protocol-output executor reports a real-socket reproduction with roughly 202 KB buffered and zero budget accounting through automatic pong responses. Parent has not yet independently run that new reproduction. Common ping/pong admission correction and healthy-client control-liveness verification are in progress.

The B1 corrective executor is released with additional parent constraints: authoritative rebase must actually be reachable for a contaminated thread; an 8 MiB event cannot silently violate a 4 MiB thread budget; configuration must not silently increase memory limits to satisfy headroom arithmetic; sender queue capacity alone is not proof of an in-flight transport bound; waiter/timer maps need global bounds; and unknown errors require an explicit finite, idempotence-aware policy. The design's contradictory or unproved shortcuts are not accepted merely because its model tests pass.

No new completion, full-green, real-renderer performance, or production-readiness claim follows from this checkpoint. Corrections, integration tests, exact-artifact qualification, and platform gates remain active.

### Transport verification and ownership moves

- Parent independently ran the real-server protocol-frame, principal-fairness, and heartbeat suites: **3 files, 21 tests passed**. This covers ping/pong admission, slow-peer isolation and transport retention on loopback. Parent corrected the stale outbound-budget comment: the 8 KiB value is a reconciliation tolerance, not permission for unaccounted ping/pong traffic. Packaged, relay, native, and sustained-load gates remain open.
- B6 reports **1,348 iOS tests and 1,254 Android tests passing**, plus 99 shared contract tests. Its correction checks monotonic retained age on append and consume and relies on existing network deadlines to close quiet buffers. Independent review is now tracing whole read attempts and every release path; per-request timeouts alone are not accepted as proof of an attempt-level retention deadline. Real rendered native qualification remains open.
- C4 reports **5,315 supervisor tests passing**, with all residual cold WSL preparation and configuration paths converted to awaited, bounded operations and staging protocol 3. Independent review is tracing production callers and cancellation/resource loading before accepting the source claim. G-WSL-1 remains open.
- E1/E3's completed graph inventory reports zero Electron runtime chains from build entries, but real shared-service imports through `src/main` and compatibility reexports remain. Browser extraction keeps the `WebContents.debugger` adapter in main while moving reusable contracts/mechanics to host. Separate scheduling, PR-watch and git-state ownership moves are assigned. Supervisor-client and app-controls moves wait for the active persistence caller changes.
- Parent reproduced an A1 overflow re-admission defect with the actual interest registry and wire-bound selector: after 201 retained threads, retaining the excluded thread updates the priority snapshot and resolves its `ready` promise, but publishes no subscriber change, so the wire selection still excludes it. Evidence: `tmp/v2-production/scratch/a1-interest-readmission.test.ts`. Restored subscriptions also need baseline recovery after a coverage gap; the server's interest-update handler only updates membership. This requires correction after A2's active routing/registry work, along with visible overload state.
- The A0 and initial A2 executors became terminal without final reports. Their existing changes are retained and scoped continuations are assigned. A0's latest raw R0 run failed before measurements because no producer was verified; observer sample counts were zero. The temporary persist-storage replacement is not accepted as normal startup and is being removed from qualification. The corrected baseline will be separately identified after the minimal product boot fix is reviewed.
- A read-only public npm lookup for `poracode` returned **E404** on 2026-09-20. This is not proof of namespace ownership or permission to publish. Packed local installation and publication authorization/provenance remain separate release gates; no npm publication was performed.

### Qualification preparation and pagination design review

Parent inspected the actual B6 result artifacts: `.tmp/ios-app-tests-full-b6-correction2.xcresult` reports **1,348 passed, 0 failed, 0 skipped** on iPhone 17 / iOS 26.5 Simulator; Android JVM XML reports **254 suites, 1,254 tests, 0 failures/errors/skips**. Parent also ran the graph analyzer's **18 tests, all passing**. These results do not close the native rendering gate or establish that every recovery buffer has an effective whole-attempt deadline.

A separate detached comparison worktree now exists at `tmp/v2-production/arms/a0-startup-corrected`, based on the original `36e1649f0`. It contains exactly the six hash-verified observer files copied from raw R0; its checkout-local frozen install passed. It has **not** received the boot correction or a measurement acknowledgment yet. Raw R0 is unchanged. The current main branch also contains the user's separate OpenCode Go usage commit `11f4480c6`; it is preserved and will be recorded in candidate provenance.

B4's first design includes useful source traces and a synthetic SQLite probe, but is not accepted wholesale. The probe found a costly runtime-summary query and confirmed cursor skips during catalog churn. Parent requested a bounded correction before implementation: prove finite reconciliation under continuing updates; do not treat a second unstable walk as complete; enforce byte limits before selecting payload columns and account for UTF-8/JSON expansion; preserve legacy summary semantics unless capability/compatibility evidence allows a change; make older completed-turn metadata retrievable; include project paging in full scope; and justify new indexes against measured write/read tradeoffs. B1's rejected oversize exception is not a valid cap to inherit. The generic oversized-item resource remains conditional, not a reason to accept oversized normal responses.

The new 2,518-line architecture analyzer also needs a responsibility review before handoff: scanner/resolution, root discovery, graph policy, and inventory/CLI output are separable responsibilities. Its current passing tests and useful inventory do not exempt newly introduced tooling from E2.

### Environment pre-integration review and artifact lock correction

Independent C1 review reproduced six pre-integration blockers: accepting one host-key fingerprint writes additional unaccepted keys; stale CAS mutations abort/disconnect live work before validation; a client's abort cancels host-owned connect; pairing can return a credential against a stale child identity; failed disconnect/join is swallowed before deleting custody records; and a legacy connection ID can collide with another environment's minted ID. It also reproduced unbounded operation admission, multiplied retry work, and descriptor-abort cleanup gaps. These are service/store findings in the uncomposed candidate, not claims about a shipped environment API. The reviewer verified existing generation checks, early trust guards, redaction, and asynchronous store custody. A corrective runtime executor is assigned with the original feature scope intact.

The B1 corrective executor and C1 runtime executor became terminal without final reports after integration/review messages. Parent verified terminal state with `get_status` and retained their partial edits. B1 continuation is split between host persistence/read fences and supervisor credit/control delivery, with explicit file ownership and a parent-owned Core callback handoff. C1 continuation consumes the concrete review probes. No completion is inferred from partial source or terminal status, and subsequent routine status checks use polling rather than steering.

The artifact correction reports a successful local real-tarball launcher/install primary phase and exact-byte matrix wiring; CI and upgrade qualification remain open. Parent then reproduced an installer-lock ABA race: another process replaces a lock after the stale-owner observation, and the stale reclaimer renames/deletes the new live owner's directory. `tmp/v2-production/scratch/cache-lock-aba.test.ts` passes while asserting this defect. A corrective executor owns lock acquisition/reclamation/release and real concurrent-process regressions.

Parent also found that the publication preflight treats successful `npm owner ls` as current credentials' write authority. [npm's owner documentation](https://docs.npmjs.com/cli/v11/commands/npm-owner/) defines it as a maintainer-listing command; the implementation never compares the authenticated caller or verifies access. The correction must establish appropriate authority or fail truthfully, preserving the separate trusted-publisher setup gate. An npm E404 remains absence of a visible package, not proof of ownership. No registry mutation, publication, or CI setting change was performed.

The user's separate Muse usage commit `5dc30b681` now follows `11f4480c6` on the branch. Both are preserved; neither was made by these architecture executors.

### Parent verification after architecture challenge

The user-facing architectural verdict preserves the shared host/supervisor design and does not claim production readiness. The review turn produced fresh evidence: native activity/transport suites **4 files / 27 tests passed**; the A1 re-admission repro still passes while asserting the defect. A1's correction now owns registry/wire readiness, baseline recovery after a coverage gap, and visible localized overload behavior; A2 has released these files.

- **A2/R0.1:** parent inspected the boot-only source diff (local-shell classification plus removal of the unserved remote declaration), applied `a2-startup-bootfix.patch` only to `tmp/v2-production/arms/a0-startup-corrected`, and verified all **108** patch hashes plus the **6** frozen observer hashes. Exact dirty/untracked status is those 114 files. Parent ran **579 focused tests** and the remote code-generation check in that arm, all passing. `a0-startup-corrected-ack.json` records every approved file/hash. Raw R0 still has exactly its six observer changes and remains untouched. R0.1 is explicitly a startup-corrected comparison, not a retrospective claim that raw R0 booted.
- **A0:** completed harness now requires genuine store hydration and removes the forced-storage/DEV-store workaround. A real corrected-arm calibration is running: producer-free trusted-input/blocking control, then a normal-supervisor structured ACP fixture. Unit/helper success does not establish measured responsiveness or real structured-process delivery.
- **E1:** browser extraction keeps the Electron `WebContents.debugger` adapter in main and moves shared tools behind host-owned contracts. Parent ran **7 files / 53 tests**, all passing. Durable-services moves and A2 integration passed another **4 files / 18 tests** including real forked-child activity, hydration, backend durable composition and headless host services. The dependency analyzer's responsibility extraction is running; remaining supervisor/app-controls/legacy ownership moves and CI enforcement are still open.
- **B6:** independent review traced real production read deadlines and found no Important timeout-safety defect. It did find wrong-thread iOS history leaving loading stuck, owner-valid iOS cancellation retaining batches, and Android history failure/cancel retaining frames. A bounded correction owns these paths and lifecycle tests. Native render/device qualification remains open.
- **B4:** parent SQL counterexamples in `scratch/b4-parent-counters.mjs` demonstrate that project manual-order continuation still skips reordered live projects; known-before minus immutable-id inventory can falsely delete a restored known id behind the cursor; and an unrestricted id walk can follow insertions ahead beyond the starting population. These invalidate specific completion/deletion claims in the correction design, not the value of progressive paging. Final design correction must also separate soft page targets from hard response limits so a valid large single item is not refused just for exceeding the small default page target.
- **D4:** executor reports **120 focused tests and 3 real-artifact integration tests** passing after journal/admission/recovery corrections; independent re-review is running. Genuine released N−1/platform/service-user/crash evidence remains separate and open.
- **C1:** parent additionally reproduced `EnvironmentRuntimeService.dispose()` resolving despite a rejected disconnect and returning the same resolved promise on a second call without retry. `scratch/c1-dispose-custody.test.ts` passes while asserting this pre-integration service-level defect, using only a throwaway store/fake transport. Composition must not infer confirmed environment cleanup from this outcome. The active runtime correction must be checked against this case before integration; no shipped orphan-process claim is inferred from this fake-transport probe.
- **D5:** a separate correction now owns graceful client shutdown announcement and actual external-watchdog proof for a synchronously wedged child. In-process timers remain insufficient proof of that stronger guarantee.

No integrated final-green, full capacity, platform, soak, publication, or completion claim is made at this checkpoint.

### Parent persistence composition integration

Parent wired `BackendHostCore` to consume the actual `PersistOutcome`: only the accepted envelope/prefix is published, explicit refusal is withheld, and the deferred reset callback publishes only after its durable rebase completes. Canonical producer acknowledgement now follows the admission decision on the original envelope. The backend database RPC dispatcher now awaits completion before announcing a database change, so an asynchronous fence/read failure cannot announce success early.

Regression coverage exercises refusal-without-publication, accepted-prefix-only publication, admission-before-credit-ack, and deferred reset publication; **3 backend suites / 30 tests passed**. Touched-file lint is clean. A full typecheck diagnostic run found no error in these integration files; it remains red in active C1/B1 test scopes and an unrelated Muse usage test, so this is not a final root check. Credit-window negotiation/headroom policy still needs the completed B1 producer/host handoffs and integrated fault verification before B1 can close.

### Completed correction handoffs and next independent checks

- B2 lost-response candidate is complete and independently reviewing. It adds transport phase evidence and per-attempt managed `startThread` receipt identity, preserving one bounded authoritative read and no automatic resend. The review specifically checks mutation cancellation after dispatch, where aborting a client wait cannot establish that the server effect did not occur.
- C4 independent review reproduced three Important cold-path defects despite the broad green supervisor suites: missing qoder structured/auth preparation, missing claude logout preparation, and an unbounded join when an aborted/timed-out child ignores SIGTERM. A corrective executor owns these provider/spawn/WSL paths, with B1 sender/manager ownership excluded. Real Windows/WSL qualification remains required.
- Artifact cache/publication correction reports real concurrent-installer tests and required-mode real-artifact upgrade **3/3** passing. Independent review now challenges the quarantine/restore lock algorithm under a third acquirer, not merely the original two-party ABA schedule. Nothing has been published or dispatched.

### Real calibration and remaining composition work

- Parent verified the analyzer extraction's **19 tests**, all passing. Same-tree output equivalence is recorded by its executor; the enforcement gate still waits for remaining prohibited ownership edges to move.
- Parent verified promotion localization with **5 files / 55 tests** and generator drift check, all passing, and inspected the real Electron German progress-window screenshot. Full promotion remains unqualified: `hostImportFiles.ts` still inventories/hashes/copies synchronously on the admission path. This is an A5 responsiveness gap separate from translated strings. The dev harness also puts live Chromium userData inside the source root, which correctly causes offline-import validation to refuse; tests must use a genuinely offline source rather than weaken that guard.
- R0.1 real calibration **passed natural boot** (`hydrated: true`, no startup/recovery surface) but did **not** pass input qualification. Recorded idle Event Timing samples are zero; frame counts are also zero, while blocked-phase entries arrive late with very large durations and ring loss. This is not evidence that idle latency is zero or that the application passed its responsiveness budget. The [W3C Event Timing draft](https://www.w3.org/TR/event-timing/#sec-performance-observer) specifies a 16 ms minimum reporting threshold and paint-dependent entry delivery. The run's missing frame/control evidence prevents explaining away the failure as fast input. Observer delivery, visibility/paint, phase attribution, and scheduled positive-control tasks need calibration before comparative measurement.
- C1 proxy transport is complete as an uncomposed candidate; independent real-parent/child authorization/resource review is running. The new management contract/handlers and shared host resource composition have separate owners. Neither partial slice authorizes advertising usable SSH environments before route/composition/client integration.

### Standalone shutdown composition custody correction

Parent found that `headlessRemoteComposition` nulled the SSH manager before joining it and caught its disposal error as success. The shared-services handle was also nulled before its join. Both handles are now cleared only after confirmed disposal; SSH failure propagates through the existing joined-shutdown contract, so outer ownership is retained and the same handles remain available to a retry.

New real-composition tests inject a one-time failed join for each handle, assert disposal rejects and a successor in the process remains refused, then assert retry invokes the retained handle and succeeds. These use real host ownership with isolated fixture resources, not a claim about cross-process kernel exclusion. Parent ran the headless composition and host lifecycle suites: **2 files / 38 tests passed**. Touched-file lint/format are clean. This complements, rather than replaces, D5's external-watchdog and real process-death gates.

### Remaining authority and recovery findings

- Parent reproduced an E1/A2 behavioral dependency behind the file layout: `app-controls/mcp/tools/threads.ts:update_thread` awaits `emitRemoteThreadCommand` before `updateThreadRow`. A pending native acknowledgement delays the authoritative metadata write; a rejected acknowledgement prevents it. `scratch/e1-update-thread-native-wait.test.ts` passes **2 probes asserting these defects**. This needs correction when the active B1 MCP-reader scope releases, with durable update independent of optional native projection. The hypothesis that MCP create/start itself still depends on the renderer was rejected: those paths already invoke the supervisor directly.
- B1 producer correction is complete. Parent's original-envelope acknowledgement hook is already integrated; the negotiated credit-window/host headroom policy and combined real SQLite/producer fault proof are still pending the host correction handoff. No claim that static sender capacity alone bounds all in-flight work.
- A3 independent review confirms transport credit retention, instance fencing, and bounded tombstones, but reproduced three residuals: Date/boxed-object serialization hooks bypass projection admission, string/key enumeration work occurs before the budget refusal, and worker recovery clears a prior lane-level loss latch without a renewed generation. A scoped corrective executor is active; no new scheduler or serialization framework is authorized by these findings.
- B6 lifecycle corrections report real short-deadline URLSession/OkHttp tests in addition to buffer tests. The Android deadline test exposed a further real defect: `call.isCanceled()` also becomes true for OkHttp call timeout, causing transport failure to be classified as caller cancellation and leaving first-load rich chat in Loading. A follow-up owns explicit cancellation provenance, timeout failure/uncertain-mutation semantics, and settled UI state. This is not waived because frame retention is now bounded.
- A5 promotion file-I/O correction now owns the synchronous inventory/hash/copy path. It must preserve offline-source verification and root custody; the live-userData refusal is not to be bypassed.

### Calibration outcome, host catalog execution, and shutdown verification

R0.1 real calibration is complete and **not a qualifying performance result**. Natural startup passed in all three isolated sessions. The structured-only session ran the real ACP fixture through the supervisor, with a ready PID/prompt marker and **2,957 canonical bulk frames / 949,341 bytes** at its interested client (offscreen bulk bytes zero). Its scripted completion failed because the renderer status gate watched an unsubscribed row; production afterAll Stop/close nevertheless reached the fixture (`cancel.marker=1`) and its PID was dead after cleanup. The combined PTY leg still produced zero recognized ticks. All owned sessions/ports were stopped. A correction owns visible structured content, authoritative status, PTY binding diagnostics, and paint/observer/blocking-control calibration; no original failure was relabeled as a pass.

B4 catalog execution now uses the final frontier + membership-confirmation design, with a parent correction to its terminology: `maxDecodeBytes` accounts serialized UTF-16 input (`2 × serialized.length`), not an invented upper bound for parsed objects, structured clones or RSS. Soft page targets do not refuse a valid page-of-one; explicit wire/decode hard limits remain. The executor must audit membership invalidation before clients stop full refreshes on ordinary thread-state events. Contract integration waits for the current C1 generator owner; history and all-client rollout remain required.

Parent ran the shutdown-announcement and real external/backend watchdog suites: **4 files / 7 tests passed**. These are POSIX fixture proofs, not Windows, live service-manager, or packaged-artifact qualification. Independent review is checking other backend retirement paths as well as `disposeAsync`.

The final artifact-lock review reproduced a production-default release race (`ENOTEMPTY`) and further reclaim/third-owner interleavings. A corrective executor must make immutable cache publication safe under concurrency; directory checks alone are not accepted as a kernel-lock guarantee. Publication preflight received no new findings.

### Consolidated integration and evidence corrections

- A1's re-admission, coverage-gap recovery, and localized capacity correction passed the parent's **4 suites / 49 tests**. The real-server loopback suite initially had 10 passing assertions but exited nonzero for an unhandled rejection. Parent traced this to the test attaching its rejection handler after shutdown, before ensuring the request had reached the server. The test now observes the promise immediately, waits for actual server receipt, starts shutdown concurrently, and releases the fixture's held operation before joining shutdown. The corrected suite passes **10 tests with no unhandled error** and exercises an actual in-flight disconnect; it no longer passes by racing fetch against a closed port.
- B1 host correction finished. Parent integrated the remaining advertised in-flight window: database binding and host headroom update precede the canonical credit grant; every supervisor advertisement renegotiates it, including clearing credit for a legacy successor. Malformed numeric bounds are not installed. Original-envelope admission acknowledgement and accepted-prefix publication remain in place. Parent verified **4 suites / 72 tests** across Core, producer control, real SQLite correction, and SupervisorClient. Independent review now covers the combined chain; sustained/fault/load qualification is still open.
- C1 runtime's original 13 probes and broad suites do not cover disposal failure. Parent reran the separate disposal probe: it still passes while asserting swallowed failed disconnect and no retry. A scoped correction owns this lifecycle. Parent also reproduced the new composition closing its store before lazy archive preparation actually settles (`scratch/c1-composition-preparation-join.test.ts`): abort alone is not a join. A separate composition correction must join preparation, reject new work after shutdown starts, and consume the runtime's corrected retryable disposal contract.
- C1 proxy review verified dual authority, generation fencing and lifetime budgets, but reproduced proxied Stop losing control admission, an HTTP query leaking the parent WS ticket to the child, encoded-control path acceptance, and redirect/header/descriptor edge gaps. The candidate remains uncomposed; these findings must be corrected before usable-environment advertisement.
- D4 independent review verified the earlier corrections and a real eight-process prefix-lock race. It also proved that the cited real-artifact tests used a candidate bundle built before those corrections. Fresh corrected-candidate qualification is required. Unknown journal phases, dangling journal symlinks, abandonment revalidation/custody policy, and inaccurate power-loss durability wording remain follow-ups from `d4-correction-independent-review.md`; the earlier 3/3 result is not evidence for corrected candidate-side behavior.
- C4 cold-path corrections finished. Parent found a further timer cleanup defect: the original deadline survives a successful child exit and later calls the raw PID termination helper. A fake-process probe reproduced that post-exit signal without touching a real process. Parent now clears the deadline on every settlement and guards termination before signaling. **3 spawn suites / 10 tests passed**, including real SIGTERM-ignoring child escalation and new success/nonzero/spawn-error/abort timer regressions. Actual Windows/WSL and broader process-custody qualification remain open.

The latest parent typecheck has no diagnostics in these integration files; it remains red in the active C1 management test file and is not a final check. The user's separate Muse CLI-auth commit `1483d5890` is preserved. No architecture commit, push, publication, or completion claim follows from this checkpoint.

Parent also verified A5's async promotion/import integration with **5 suites / 76 tests**. The raw measurement files describe the same 2,009-file / 276,627,589-byte fixture and identical inventory digest before and after. The synchronous copy's maximum heartbeat gap was 847.1 ms; the latest async run's maximum gaps were 5.82 ms inventory, 6.11 ms copy, and 6.50 ms large-file hash. Elapsed duration varied (the latest async copy took 1,194 ms versus 846 ms in the recorded sync baseline); the demonstrated benefit is event-loop availability, not a claim of faster copying. These isolated measurements do not replace the packaged-main promotion trace.

The final B1 integration check is **4 suites / 73 tests** after adding the synchronous pressure-transition case: a headroom update that itself emits a pause now carries the new peer's credit, never the previous boot's grant. Touched-file lint/format pass. `tmp/v2-production/b1-parent-integration.json` records this handoff's source hashes for the independent review.

### Next consolidated corrections from independent reviews

- D5 review reproduced an actual orphan on initialization failure: `BackendHostClient` sends TERM and drops the retiring child reference before respawning; later disposal only joins the successor, while the old child can still own its port/fence. A scoped correction now owns every backend retirement path and a real TERM-ignoring child regression. The existing normal disposal and HTTP handler-drain claims were independently verified; this finding does not invalidate those narrower proofs.
- B2 review reproduced post-dispatch caller cancellation being composed as `requestMayHaveCommitted: false` because a duplicated status rule treats local status 499 like a server rejection. Current mutation entry points do not pass a signal, so the defect is latent in the new transport contract, not a claim of an observed duplicate user effect. A correction owns one shared classification rule and presend/dispatched/read-cancellation parity tests, without automatic mutation resend.
- C1 proxy corrections now own the complete verified admission/credential/path/redirect/header set from the independent report. Management routes, composition, and all client surfaces retain separate ownership; capability advertisement remains off until the usable path is integrated and verified.
- A3 correction finished and records seven regressions failing against pre-fix code before passing. The code explicitly disclaims a hard heap bound for engine-owned key enumeration; replacing `Object.keys` with `for...in` is not treated as proof that the JavaScript engine avoids upfront enumeration work. Transport-retention credit remains unchanged. Broader-suite noise is not waived as a pass; the loopback shutdown test correction above addresses the independently reproduced test-order defect.

Parent independently reran the corrected A3 engine, persistence, browser metadata-cache and hydration suites: **8 files / 130 tests passed**. The active goal remains incomplete: environment integration and client rollout, bounded catalog/history rollout, remaining ownership moves, fresh artifact qualification, performance matrix and platform/device gates are still required.

### Host environment composition and consolidated verification

The parent has wired the environment runtime into both actual server compositions. Desktop main forwards the staged assets; the backend adapts its existing data fence and owns the environment manager independently of the device-local SSH utility. Headless borrows its existing SSH manager and joins environments before that manager's disposal. Both start bounded reconnect work without blocking the local listener on remote reachability. Failed joins retain the owning references and root custody.

The 13 management dispatchers now call the real runtime factory with scoped HTTP cancellation. A shared composition helper binds management, verified proxy targets and parent authentication to the same authorities. The descriptor advertises `sshEnvironments` only when management and proxy are composed; an absent/partial composition stays unavailable. The server itself now disposes its proxy gateway before joining transport/work teardown, instead of relying on fixture or caller cleanup. Client surfaces and the renderer/native environment records are still unimplemented.

Parent verification: initial backend/headless/management/initialize **9 suites / 103 tests**; desktop controller **3 / 15**; real-fence legacy/asset/standalone descriptor-list checks **3 / 13**; environment/relay/composition/cancellation sweep **10 / 94**; final real HTTP management integration **5 tests**, including an active upstream stream closed and joined by server disposal alone. Initial composition typecheck passed. A later check is red in the active B1 refusal correction, with no parent integration diagnostics. Exact handoff and limitations: `tmp/v2-production/c1-parent-composition.md`.

Other completed results were checked separately: parent reran launcher tests **29/29**; Android result XML contains **255 suites / 1,271 tests, zero failures/errors/skips**. These do not establish Windows, physical-device, published N−1, or full load qualification. Cache correctness now rests on private staging and atomic immutable publication; the directory lock is explicitly only a duplicate-work optimization.

The integrated B1 independent review verified the credit/persistence chain but reproduced typed-refusal HTTP mapping gaps and abandoned fence pins during shutdown. It also demonstrated a global-refusal recovery path with no visible per-thread gap evidence. Parent treats that last observation as a required correction under the no-silent-canonical-gap invariant; the corrective executor owns all three. B4 history reads are being implemented in separate modules against the existing fence seam, while bounded-read advertisement waits for the complete catalog/history/turns contract.

Further bounded work is active: independent composed-environment review, client authority/resource-path design, fresh D4 journal/artifact qualification, C4 cancellation/successor review, and app-controls metadata updates that must not await Electron acknowledgement. Large ownership moves wait for these behavior boundaries and artifact work to settle.

Parent additionally reran the backend retirement unit + real-child regression: **2 files / 36 tests passed**. C1 client research confirms the headerless browser-image gap and parent-pin/token-lifetime seams. Its proposed auth retry ordering and bare environment-ID record key are not yet ratified: parent requested a bounded correction covering recursive refresh, trusted error provenance, parent-scoped connection identity, explicit blob readiness notification, and the distinction between child-host management and another nested data-plane hop. No client implementation may infer those decisions from the initial report's “settled” label.

Completion also requires the full plan §4 envelope, not just the numbered implementation rows: configurable host agent/resource admission with measured defaults, the 1/8/32/64-thread and multi-client/device matrix, 10,000-row catalog behavior, fault cases, 30-minute sustained load and overnight soak. Principal HTTP/socket admission does not by itself establish an active-agent process limit or prove those performance gates.

The final C4 lifecycle review independently verified three residuals: an aborted single-flight can admit a successor while the prior extraction still writes; existence-only `bin/node` acceptance can preserve a partial failed install; and executor eviction/idle-timer races can affect a successor. Correction is assigned to those exact lifecycle/publication paths. The review rejected the proposed production reachability of the defensive unconfirmed-exit path and the stdout/stderr hang hypothesis; those are not used to justify extra mechanisms. Actual Windows/WSL qualification remains open.

### Fresh artifact, host metadata integration, and client rollout

D4 correction evidence now uses newly rebuilt bytes: `poracode-server-1.8.1-darwin-arm64.tar.gz` SHA-256 `3faa4918d079c0e03ad087870a77a3d0ec042b2db4515dfcb5cda0b16cdb3cf7`, embedded server SHA-256 `4ef4ecc63bc158f121b495da40ce01a99d67730b60cb90acca93d08a30eb13e1`. Executor reports 130 unit checks, 3 required-mode real-artifact integrations, 4 external startup/doctor probes and a real SIGKILL at `candidate-started` followed by successful artifact `--resume`. Parent inspected the exact-artifact report; this is D4 scope evidence, not qualification of later concurrent source changes. No published standalone N−1 artifact exists in the inspected releases, so the local byte-distinct candidate is not called N−1. Journal writes provide process-crash atomicity, not power-loss durability. Unusable-journal abandonment remains an explicit operator override, never evidence of prior candidate admission.

The composed C1 review reproduced a committed create returning `store-unavailable` during shutdown and a headless cleanup path skipping the manager's strongest join after an environment handoff failure. Correction is assigned to those outcomes. Review verified custody reuse, honest capability advertisement, actual management/proxy composition, and active-leg teardown. A trusted parent-auth rejection marker plus stripping of the child response namespace is the agreed prerequisite for transparent client recovery; status codes or a separate liveness probe cannot establish which authority rejected a request.

Client design now uses a local connection key separate from child host identity, live parent token/pin lookup through the parent connection, independent child grants, bounded paired WebSocket tickets, and subscribed authenticated image resolution. Child-host environment management through one proxy hop remains supported under child scopes; creating an additional proxied data-plane hop is explicitly refused. Shared SDK, iOS and Android implementation lanes are active. Renderer integration waits for the SDK handoff.

E1 app-controls metadata updates now commit independently of the optional native mirror. Done/archive effects run on the host, and durable writes publish membership invalidations after commit. Parent wired `publishThreadsChanged` into both desktop and standalone compositions. Parent verification: **7 files / 67 tests passed**, covering app-controls, durable/desktop/headless composition, schedules and shared metadata semantics (`tmp/v2-production/e1-parent-composed-verification.log`). This does not yet close the remaining dependency ownership moves or static CI enforcement.

B1's consolidated correction implements typed HTTP refusals, shutdown fence-pin release, explicit per-thread contamination after global canonical refusal, and synchronous mutation-callback enforcement. An independent follow-up is active; full fault/platform and durable restart evidence remain open. Executor claims of unrelated full-suite failures are not treated as established pre-existing defects without baseline comparison.

B4 history and catalog candidates are not advertised. Parent adversarial probes reproduced two false hard-cap refusals: a valid 4,000,000-character Chinese stream was rejected using a 72 MB upper estimate despite its approximately 12 MB serialized wire content; a roughly 2 MiB configuration was rejected under a 4 MiB client budget using a roughly 12 MiB upper estimate. An upper bound above a limit is not proof that the actual value exceeds it. Consolidated correction covers hard-refusal proof, bounded materialization, shared legacy/bounded history classification, reuse of persistence refusal mapping, and responsibility-based extraction of the new 1,068-line history server module. Parent probes are preserved under `tmp/v2-production/b4-*upper-bound*`.

### Persistence integrity follow-up and WSL completion boundary

The next independent B1 review verified the typed 503 mappings with the real persistence gate and HTTP server, shutdown pin release, in-process global-gap visibility and receipt classification. It also reproduced a dropped deferred reset allowing later new-session items to append to pre-reset history, plus a missing runtime callback check on the synchronous mutation path. Parent treats the reset failure as required canonical-integrity work. Parent added two independent probes: a failed delete discards accepted buffered events before the SQL operation succeeds; and an age-refused batch's gap marker disappears after normal database close/reopen, allowing the shorter history to be served as complete. The latter is now reproduced, not merely an untested crash hypothesis. Correction covers failed-delete/reset behavior; durable restart evidence needs an explicit versioned design that remains truthful when storage itself is unwritable. A schema change being required does not waive the plan's no-silent-gap invariant.

The C4 install/lifecycle correction reports **35 focused tests** and **175 affected tests** passing, including real POSIX extractor termination before successor entry. Shared flights retain custody until task settlement, archive staging names are unique, and executor retirement checks entry identity. Actual Windows/WSL remains unqualified. Parent found a remaining version-boundary defect: the new completion marker writes a version and identity but its reader only checks existence. A narrow follow-up validates the marker and distinguishes absent, corrupt, future-version and mismatched identity without deleting prior installations. Broad supervisor test counts do not substitute for that check.

The latest E1 source audit still has **20** reverse-boundary findings (including compatibility paths). Moves are staged for actual owners after active source corrections settle. Parent's source trace additionally shows the backend writes managed-loopback TLS pin state in its own process while Electron main reads its separate copy; the bootstrap currently forwards only endpoint/pairing URL. TLS-configured managed bootstrap needs targeted reproduction and explicit native trust handoff before relocating that controller. No certificate verification has been relaxed.

### Shared SDK handoff and renderer integration

C1 shared SDK now provides the 13 bound-host management methods, marker-attributed parent recovery, separate child lifecycle, and exact child-to-parent WebSocket ticket pairing. Runtime environment-client imports deliberately use their dedicated module because a barrel runtime re-export creates a class-initialization cycle; type-only barrel exports preserve discoverability. Parent reran the management/recovery/ticket tests: **3 files / 29 tests passed** (`tmp/v2-production/c1-sdk-parent-verification.log`). Native implementations are active, and the renderer store/UI/image executor now consumes the concrete SDK interface rather than the earlier proposal.

Parent's image integration review found the proposed React `useSyncExternalStore` example unsafe: the SDK getter constructs a fresh object on every read. The renderer slice must use a stable primitive/version snapshot or fix cached snapshot identity, with a real finished-thread component test. That slice also owns correcting the cache's claimed LRU behavior and verifying in-flight byte/concurrency/deadline bounds and auth recovery; ready-blob cache size alone is not an in-flight-memory limit.

The parent notification callback wiring's readonly-array type mismatch was corrected by copying affected IDs into the event payload. Both touched composition files pass lint/format. The subsequent full TypeScript check is still red only in the active B4 correction's evolving metadata test fixtures; no global-green claim is made. Earlier staged ownership moves remain in the index alongside unstaged follow-up edits and must be preserved during final commit grouping.

TLS trace refinement: the managed pairing URL already carries the server fingerprint; the renderer bootstrap extracts only the credential, and main does not install that existing trusted pin evidence. The prospective fix should reuse the authenticated bootstrap URL if sufficient, rather than adding a redundant wire field. This remains a source-backed composition finding awaiting a targeted TLS reproduction.

### Real Electron calibration outcome

A0 reran against the unchanged 114-file approved R0.1 arm. The corrected preflight measured **69** interaction samples with **zero** ring loss and three actual 250 ms DOM-handler blocks; queued trusted input delay was **250.2 ms**, versus a **1.9 ms** idle page-side maximum. Structured calibration verified a declared visible chat pane, **3,994** canonical GUI frames distinct from **2,312** terminal frames, **zero offscreen bulk bytes**, and in-body Stop → cancellation marker → settled status → close → child PID exit. Failed intermediate runs and their concrete harness diagnoses remain preserved.

This is functional calibration only: host load reached **27.1**, with concurrent build activity, and `capacityClaimAllowed:false`. The successful combined cell also disabled the visible terminal pane to avoid app-owned provider replacement of a harness shell, so it does not satisfy the required two-chat-plus-visible-terminal workload. A bounded harness follow-up owns real visible terminal binding and segmented trusted-input windows that preserve the full measured population under the frozen 256-entry ring. Full capacity, load/soak and device gates remain open.

C1 server follow-up passed parent verification: **5 files / 27 tests**, covering committed-create disposal, strongest SSH cleanup/custody retry, actual desktop descriptor/management composition, auth marker provenance/spoof stripping, and relay forwarding (`tmp/v2-production/c1-server-parent-final-verification.log`). The implementation returns the committed create projection directly and attempts the manager cleanup independently of a failed environment handoff. The remaining E1 move/enforcement executor is now active on the released paths; it must preserve native certificate authority and verify the managed TLS handoff rather than simply relocate a process-local pin variable.

A0 coordination note: follow-up run `171d9efea33c` ended `completed` with empty output after an accepted steering message; no report was produced. Remaining work was dispatched as a complete standalone brief to `990c775548d1`. This is an observed coordination result, not evidence of a provider outage. Parent also recorded a typing-fidelity finding for final harness review: the inspected idle screenshot shows doubled characters, matching a helper that sends both keyDown-with-text and char for each declared character. That path needs an exact editable-text assertion before numeric workload claims.

The C4 marker correction now validates its version, pinned Node version and target via the existing staging read operation. Future marker versions fail before any reinstall/stamp; read failures propagate rather than becoming “absent”; corrupt or mismatched markers require exact-binary revalidation before repair. No cache layout, Node path or staging protocol changed. Parent reran the combined marker/install/single-flight/staging suites: **5 files / 84 tests passed** (`tmp/v2-production/c4-parent-final-verification.log`). The host-resource-admission research lane is also active against the launch and actual-retirement boundaries; defaults remain unratified pending uncontaminated workload evidence.

Parent added a real HTTP/proxy SDK integration regression (`src/host/remote/environments/environmentClient.integration.test.ts`): expired parent/valid child, valid parent/expired child, and both expired all pass (**3 tests**). The parent is the real `RemoteAccessServer`/gateway, both grants use real auth stores, and refresh traverses real HTTP; the child is an isolated loopback endpoint using the production auth implementation. Assertions pin exact grant rotation counts, one successful child read, and absence of the parent credential header at the child. This closes the SDK report's unit-only recovery gap for these three cases; it is not a live SSH or browser CORS/physical-device qualification. The new test is in the worktree, not committed yet.

The B1 follow-up implements failed-delete preservation, synchronous mutation-callback checks on both paths, and explicit `rebase-dropped` contamination after a refused/exhausted reset. Durable-gap research is not yet ratified: its proposed best-effort dirty write occurs after refusal, so it cannot cover a crash when storage rejects both the dirty write and the gap record. A writable check after restart also cannot reveal the earlier failure once storage recovers. Parent requires a durable arm before canonical admission, explicit clean/unclean epoch semantics, bounded affected-thread recovery and atomic repair. The original credit ACK is a producer flow-control acknowledgement, not a durable-commit acknowledgement; it must not be misrepresented in that design. No schema migration has been applied by the research lane.

Parent verification of the final B1 delete/reset/callback corrections: **3 files / 29 tests passed** (`tmp/v2-production/b1-parent-canonical-verification.log`). Durable restart behavior remains a separate open implementation, with its revised design under review.

### Resource lifetime core and durable-gap integration decisions

Resource research verified three lifetime defects with the real session manager: a map entry disappears while disposal remains pending; rejecting structured disposal skips PTY termination; and same-ID shell replacement can spawn before predecessor exit. A bounded supervisor core implementation now owns execution-slot reservations across pending/active/retiring states and confirmed retirement, including structured and one-shot subagents. The quota is explicitly not a claim that logical sessions equal OS processes; provider pools and WSL memory remain separately measured. Same-slot sequential replacement must work at a full cap without overlapping unconfirmed processes. Cross-process error/receipt classification and surfaced settings are separate pending integration, with no measured default yet.

The revised durable-gap prototype passes 13 scratch scenarios, including a marker write failure followed by SIGKILL and recovered storage, and an unrepaired older-epoch touch surviving intermediate clean boots. Production wiring is still not ratified: the current lazy connection bind could write during a read, metadata caches need explicit bounds, and a never-touched thread's first refused batch needs a source-backed continuity decision. A final bounded design pass addresses these exact issues; no migration has been applied. Global receipt overrides based only on a resource error code are also not authorized—compound host operations may already have effects before the supervisor refuses a launch.

Parent reran both frozen B4 false-oversize regressions after the host corrections: **13 catalog tests + 11 history tests passed** (`b4-parent-catalog-correction-verification.log`, `b4-parent-history-correction-verification.log`). The corrected code uses sound lower bounds to prove refusal and keeps upper bounds for packing estimates. The ownership audit independently reports **0 findings** across 5,130 files, with **19 analyzer tests passed** (`e1-parent-boundary-verification.log`); its 12 unresolved computed specifiers remain explicitly ambiguous, not evidence that their targets are unused. Independent review of moved-service test causality and native TLS lifecycle is still running.

The parent ratified durable-gap production implementation with an eager runtime-owned epoch, read-only bind/resolve, and a pre-dispatch launch touch. A reset/replace must atomically preserve an existing current-boot touch, closing the live-producer gap after rebase; older-epoch repair can clear normally. Both derived metadata and pending obligations are bounded, and overflow must not spill into an unbounded contamination map. Pending evidence remains tied to its database identity. The exact decisions are in `tmp/v2-production/b1-parent-implementation-decision.md`; migration and real crash tests are now implementing.

C1 iOS candidate completed. Parent independently read `.tmp/c1-ios-final.xcresult`: **1,396 passed, 0 failed, 5 skipped**, iPhone 17 / iOS 26.5 simulator. This is test-bundle evidence, not physical-device qualification. Independent review is active; the executor explicitly leaves Advanced Operations on a raw single-credential transport, which remains required integration work rather than complete iOS parity.

Parent reproduced three SDK error-attribution defects: parent ticket network failure becomes a 401 repair error, missing-marker 401 is asserted to be child-origin, and parent-refresh transport failure loses its original classification. **3/3 expected-behavior probes fail** in `c1-auth-attribution.probe.log`; the frozen probe and consolidated review handoff are under `tmp/v2-production/`. Corrections wait for the renderer/image owner to release the shared SDK file; no active code was overwritten.

Parent verified the completed renderer candidate with **5 files / 29 tests passed** (`c1-renderer-parent-verification.log`): connection migration, sessions, management, settings gating and image readiness. Independent renderer review is active; SDK error-attribution corrections own separate files. Android candidate also completed; parent parsed its result XML: **264 suites / 1,317 tests / 0 failures or errors**. Independent Android review is active. Its separate push transport and persisted-capability freshness remain declared integration gaps; the translation subagent used by the executor is not independent review evidence.

Independent E1 review confirmed the moves/resource closure but correctly attributed failing Core/headless mocks to the E1 import rewrite. Four mock migrations remain required after the active B1 owner releases those tests. Parent removed completed `MOVE_BATCHES` metadata: architecture check and **19 tests pass**, format clean. The proposed transitive-consumer diagnostic expansion was declined because the offending barrel itself already fails CI; it does not close an escaping dependency. Rejected-bootstrap pin retention remains an exact-origin/leaf limitation, with live Chromium proof pending. Decisions: `e1-parent-review-decisions.md`.

Linux qualification is locally feasible: parent built disposable image `sha256:9de13b8d064dca136626a295c5e0584b3433eaa5a2a6a4605f46343497fcff6b` and booted **systemd 255.4-1ubuntu8.17 on aarch64**, `is-system-running=running`, **0 failed units**, private cgroup namespace, **no host mounts**. The preflight container was stopped and removed. This establishes the harness only; it is not Poracode service/artifact evidence. Logs and Dockerfile: `tmp/v2-production/linux-service-harness/`.

SDK attribution correction passes parent verification: frozen **3/3** and real HTTP expiry **3/3**. Parent found and corrected one residual assumption in image replay: a successful earlier refresh cannot prove the source of a later markerless 401. Both child-refresh and parent-refresh replay regressions now require unattributed errors, and **3 suites / 43 tests pass**, touched lint/format clean (`c1-auth-attribution.parent-image-correction.log`). No wire changes.

Independent renderer review proved F1–F8: missed connection keys in resync/refresh/connecting state/new-thread/rename, metadata edits clearing credentialRef, token-vault collision and dropped image subscriptions. All are required corrections, now executing with gallery readiness. Separate read-only research checks managed Electron settings: its ephemeral local parent is absent from the persisted remote-server list used by current environment management. That potential topology gap needs an explicit narrow design, not manual self-pairing as an assumed workaround.

The independent resource-core review verified normal admission/handoff ordering but reproduced two important cleanup defects: watchdog disposal loses its handle before confirmation, and failed unpublished/close/cancel cleanup retains only an unreachable numeric lease. Corrections must retain retryable handles, keep timeouts distinct from exit, and enforce presentation capabilities for nonstandard clients. The configuration design also needs correction: a global429 mapping contradicts an uncertain compound-command receipt on its first response, and invalid settings currently normalize to unlimited. These are required integration fixes, not measured-capacity evidence.

Independent iOS review confirmed storage/journal/pin/ticket strengths but found dual-authority omissions across multiple production transport factories, permanently stale capability gates, and identity-change errors falling through to snapshot/socket retry. Consolidated correction is active, including surfaced-error localization.

Concurrent user work advanced HEAD to `b74e11702 fix(renderer): stop painting live Crossagent runs as Failed`; preserve it. The index returned to 31 entries after that commit; never restore the earlier38-entry snapshot over the user commit.

B1 migration48 candidate completed and is under independent review. Parent reran the unchanged restart-gap probe: **4/4 pass** (`b1-parent-durable-reopen-verification.log`). The four E1-affected composition suites now show **3 files passing / 1 failing** (40 pass / 21 fail); the remaining headless factory still has duplicate canonical DB mocks and lacks the new durable-attach hook. A focused fixture correction owns only the two headless test files; Core tests already pass. No production tolerance for a missing database is authorized.

Parent verification of renderer F1–F8 corrections: **5 files / 32 tests pass** (`c1-renderer-parent-corrections.log`). Managed-parent implementation now owns the released renderer connection files. The prefix-only design was rejected because accepted host IDs are arbitrary strings; the ratified variant uses explicit managed-vs-connection discrimination and disjoint grant subjects. This also closes direct-ID/remote-environment grant aliasing and late refresh writes after remove/re-pair, with legacy v1 direct keys preserved.

Android independent review confirmed the core custody/transport design but reproduced transient UI state reset, parent-unscoped environment selection, and missing push authority. Parent also requires the reported endpoint-alias grant collision, stale reconcile/endpoint behavior, and capability-refresh omission to be closed; describing a feature as hidden until re-pair is not production freshness. Consolidated Android correction includes background push removal/outbox and parent re-pair, with actual boundary/version tests.

The headless mock correction passes parent verification: **4 files / 61 tests passed** (`e1-parent-mock-final-verification.log`). This closes the E1 canonical-DB mock migration finding without changing production lifecycle behavior. B4 contract/host integration is complete and independently reviewing; all-client pagination adoption remains pending until the C1 owners release those clients. The B4 report's claim that headless membership publication is still missing must be checked against the already-integrated E1 callback wiring before adding anything.

Independent B1 review confirmed migration/bounds/crash/transaction guarantees but proved two blockers: startup constructor cleanup released its fence on close refusal, and GUI threads had no reachable authoritative recovery after an unclean epoch. The constructor correction is complete and undergoing parent verification. A separate bounded follow-through checks standalone composition: a failed constructor may leave `backendHostRef` unset, so outer disposal must not report completion and release the namespace over an open DB. GUI recovery is in source-based design; no automatic empty reset or fabricated reconstruction is authorized.

Parent reproduced and fixed a real SQLite project-deletion failure: aborting `project_notes` deletion previously left the project and cascading threads deleted. Both SQL deletions now commit atomically; only successful commit clears pending runtime obligations. The trigger-based regression verifies failed deletion preserves records and queued work, then retry removes both. Parent startup/custody/deletion verification: **5 files / 15 tests pass** (`b1-parent-startup-delete-verification.log`). The standalone failed-constructor custody handoff now retries database close in the composition disposal barrier; persistent refusal retains the namespace lease. CLI termination semantics are being checked separately, not inferred from ownership safety.

Managed-parent and Android C1 correction candidates completed and have independent focused reviewers. B4 independent host review found an absent-`reads` admission bypass via legacy `threadLimit`/`runtimePage`, omitted stored-byte charge columns, and normal completion counted as abort; consolidated correction is active. Existing E1 membership publication is already wired and must not be duplicated. GUI-gap acknowledgement remains in design until old-reader visibility, receipt identity across successive gaps, and concurrent producer ordering are settled.

A0 final R0.1 calibration captured real structured text advance and integrated-terminal DOM output, with Stop/cancel/idle/close/PID join verified. It is **not** numeric or capacity qualification: 172 unread Event Timing entries were lost and every load sample was contaminated; budget verdicts remain null. Terminal proof required a declared production-path restart and procedure input after trusted typing did not reach the PTY. Current-source terminal causality is being investigated before any fix; a separate harness correction addresses parent-observed doubled composer typing. Frozen arms and all failed-run artifacts remain unchanged (`a0-final-harness-calibration.md`).

Parent independently reran the managed-parent/custody/loopback fixtures: **4 files / 40 tests pass** (`c1-managed-parent-parent-verification.log`). Android correction result XML independently totals **269 suites / 1,350 tests / 0 failures, errors, or skips**. These are not device or SSH transport evidence; both candidates remain in focused independent review. Project-deletion touched-file type-aware lint and format are clean.

Parent correction to the preceding A0 loss claim: the reported172 unread events are exactly the172 samples already retained BEFORE the phase. Raw counters show453 new delivered records and453 records in the segmented union. The current formula omitted the initial retained count; the actual-artifact regression fails expected0/got172 (`a0-parent-ring-accounting.before.log`). Preserve all raw evidence; correct accounting before eligibility is assessed. Host contamination, doubled typing and terminal workaround limits are unchanged, so no performance claim follows from this correction.

Resource PHASE1 parent run found **31 pass / 1 fail**: an atomic settings replacement left the effective agent limit at2 rather than1 for the two-second verification window. The isolated three-test rerun passed. This is an unresolved watcher/test race, not verified eventual propagation; evidence is in `host-resource-admission-parent-runtime-watch.md`. HTTP/receipt/metrics and localized configuration UI now have separate PHASE2 owners; policy/cache has independent review.

Android correction review confirmed push authority/outbox versioning, capability freshness, endpoint derivation and grant isolation, but reproduced a new shared-revision bug: list refresh and mutation completion can strand each other in permanent busy/loading states. It also verified a non-atomic revision-check/apply window in authority reconciliation. Both are in one bounded correction; push cleanup remains conditional on available credentials, not a claim that missing grants can unregister a remote device (`c1-android-correction-review.md`).

Parent verified the completed B4 SDK primitives: **22 real-HTTP-fixture tests pass** (`b4-sdk-parent-verification.log`), with all-client pagination adoption still pending. Parent combined CLI startup-deadline/B4 legacy-admission/charge checks: **4 files / 28 tests pass** (`host-b4-cli-parent-verification.log`). CLI failure now arms the existing configured deadline before awaiting cleanup, retaining the lease until confirmed cleanup or actual exit; synchronous event-loop blockage still requires an external watchdog.

Independent managed-parent review reproduced stale persisted-parent callbacks resurrecting a removed grant/overwriting re-pair, same-endpoint stale pin/client reuse, and concurrent descriptor retries overwriting ready with failed. Consolidated correction is active, preserving routine token rotation and child image/session continuity (`c1-managed-parent-review.md`).

Parent real-HTTP/SQLite follow-through found **2/2 false reservation refusals** after the B4 admission fix: a huge third thread beyond the requested first page and its lookahead, and a huge scrollback explicitly omitted by the request, both cause503 although neither is read. Frozen tests/log: `b4-parent-selection-charge.probe.test.ts`, `b4-parent-selection-charge.before.log`. The byte reservation must follow selected rows/options as well as actual columns; consolidated correction is active while the new admission and abort fixes remain.

Terminal causality review rejects the earlier hydration-timeout proposal: preserved PTY logs show a third same-id shell spawn destroyed the marker generation, caused by the harness calling startShell outside shellStartRegistry and a later deferred panel start replacing it. Current renderer/feed files are unchanged from the frozen arm. The first-fit miss and pre-replacement live-paint gap still need surface-side evidence; a zero-width decoration canvas is not proof of a zero-width terminal. No production timeout was added (`a0-terminal-investigation.md`, 9 scratch probes).

Resource settings parent checks: **2 files / 35 tests pass** (`host-resource-admission-ui-parent-verification.log`). A small copy correction removes internal measurement-status prose from the three controls; `0 means unlimited` is the user-facing rule, while measured-default qualification stays in the execution log. Parent also corrected the standalone docs table to the source-backed loopback default127.0.0.1 and clarified that manual edits target the same document rather than passing through the settings authority. Runnable service/TLS examples still await exact-artifact qualification.

Independent PHASE1 review reproduced the policy-cache race with real Node/macOS watchers: closing/recreating the directory watcher from its callback misses atomic replacements and leaves stale policy indefinitely (real-cache6/10 stalled runs; long-lived watcher controls had no misses). Correction keeps a single directory watcher, with separate initial-attach/error-path validation and bounded diagnostics. Other config/IPC/status invariants verified (`host-resource-admission-phase1-review.md`). Linux preparation produced image `sha256:fa1fb025828d454b4e3f930e03973a04afa7a58309d55973ed27a76a4b03b3fb`, verified non-root systemd restart/crash/stop, native addon loading and container-only SSH; no task containers remain. This is environment evidence only, not candidate qualification.

Android bounded-read adoption is now executing in released catalog/history/session paths, disjoint from the remaining two-file C1 environment-race correction. It consumes the ratified frontier inventory and confirmation-gated deletion contract, not the older unstable paint-walk proposal. Native parity remains planned until the required real-device evidence; generated bindings alone are not adoption.

E3 parent completed the per-path public/package check for three SSH shims: root package is private with no source exports, shipped desktop files are dist-only, CLI package exports no source aliases, and each shim has zero source/build consumers. Removed main/ssh `runtimeBundleFiles.ts`, `runtimeBuildManifests.ts`, and `SshConnectionManager.ts`; retained `runtimeBundle.ts`, the real tsdown/mobile packaging entry. Implementations remain under host/ssh. Evidence: `e3-main-ssh-shim-evidence.md`; current versioning-doc pointer update waits for its active owner.

Post-removal architecture audit: **5,186 files / 25,682 edges / 0 findings**, with the same12 explicitly unresolved computed specifiers (`e3-shim-removal-audit.log`). Parent independently inspected the prepared Linux image digest/architecture and confirmed no running poracode-v2 task containers.

Execution-limit copy correction also applied to the three settings-search descriptors by parent; they now reuse the same translated msgids as the controls. Fresh extraction: **0 missing in all12 non-English locales** (`host-resource-admission-search-copy-extract.log`), touched search-index lint/format clean. No new behavior or localization fallback introduced.

Parent independently read `.tmp/c1-ios-corrections-final.xcresult`: **1,421 total / 1,416 passed / 0 failed / 5 skipped**, resultPassed (`c1-ios-corrections-parent-summary.json`). Focused correction review is active. The implementation report explicitly discloses that its parent-id-only push unregister entry cannot authenticate after parent removal and is dropped on401; this remains required review/correction, not closed parity. No physical-device/FCM/APNs/realSSH evidence is inferred.

GUI gap recovery now has a ratified execution contract (`b1-gui-recovery-implementation-decision.md`): durable notice, explicit acknowledgement, unchanged saved transcript, existing B2 receipts, no false producer-retirement claim. The parent rejected an ever-growing episode ledger and deleted-thread id set: migration49 will add UUID identity on the existing gap row plus one notice row per thread, with bounded DB-backed derived lookup and delete/reuse invalidation. First DB/Core implementation is active; HTTP/WS capability/content gating and all-client notice rendering follow after overlapping owners release. The real-WS design probe disproved resync-only enforcement; old sockets must have canonical content gated while sequence positions remain contiguous. No main-local preload fallback is authorized.

E2 A/B and D/E/F candidates completed: the proxy gateway is now469 lines with explicit policy, leg/agent lifetime and descriptor identity owners; environment runtime state/custody and bounded internal CAS writes have dedicated modules. Public APIs and wire/persisted shapes are unchanged. Focused independent extraction review is active; the work is judged on preserved ownership/order and reduced mixed responsibilities, not line count alone (`e2-environment-state-extraction.md`, `e2-proxy-responsibility-extraction.md`).

Parent targeted verification of the extracted environment-state, CAS-write and proxy-leg owners: **3 files / 34 tests pass** (`e2-extractions-parent-verification.log`). Independent move/ownership review remains active; final current-artifact closure is separate.

Resource PHASE2 parent real-HTTP/SQLite refusal + metrics verification: **2 files / 15 tests pass** (`host-resource-admission-phase2-parent-verification.log`); independent review is active. Parent consolidated the duplicate docs-count guard into `protocol/remote/v3/docs-counts.test.ts`, preserving its unique protocol/binding-version checks. Historical V4/V5 dated counts remain unchanged; only current architecture/release docs are compared to the latest inventory. Both suites ran in the normal node Vitest project, so the canonical guard remains in CI. Before:2 false failures comparing dated81-route rows to current84; after:canonical guard passes, touched lint/format clean (`docs-counts-before-consolidation.log`, `docs-counts-after-consolidation.log`). Removed redundant `src/server/docsCountsDrift.test.ts`.

Independent E2 extraction review found **0 Important issues and 1 Nit**: an unused exported descriptor-path constant. Parent made the constant module-private. The review independently recovered pre-extraction bundle sources and verified state/custody, strict/internal CAS, abort ordering, identity/header policy and proxy join ordering; **113 focused tests passed** (`e2-extraction-review.md`). This does not qualify packaged closure.

Parent verified managed-parent correction against actual store/incarnation and loopback suites: **4 files / 42 tests pass** (`c1-managed-parent-final-parent-verification.log`). Stale callbacks cannot revive a deleted grant or overwrite a replacement pairing; descriptor retries single-flight and preserve current runtime metadata. Renderer/Web/PWA B4 rollout now owns the released client paths.

The frozen B4 reservation-selection counterexamples now pass unchanged: **2/2** (`b4-parent-selection-charge.final.log`), previously both false503. Charge follows selected rows/lookahead and omitScrollback/runtimePage options; validation precedes reservation. The executor also verifies selected oversized rows and all unbounded completed turns still refuse rather than truncate (`b4-legacy-selection-corrections.md`).

Parent settings-cache verification: **4 files / 53 tests pass** (`host-resource-admission-watcher-final-parent-verification.log`). One directory watcher survives ordinary invalidations; a one-shot500ms cache invalidation covers the observed initial-registration loss window. Parent corrected the source comment: this is not a watch-readiness guarantee for registration delayed beyond500ms. Failed registration warns once per episode; explicit settings invalidation remains available. No per-start synchronous settings read or measured-capacity claim added.

Independent iOS review confirmed required C1 corrections: enqueue dependent push unregisters before parent cascade deletion with bounded captured dual authority; distinguish local/parent auth failure from child rejection; fence delayed capability responses against same-identity re-pair; preserve known capabilities when re-pair describe fails. Consolidated correction is active (`c1-ios-correction-review.md`). The reviewed simulator suite passed47/47; physical-device and actual push-delivery evidence remain open.

Android C1 race correction completed (`c1-android-final-race-corrections.md`). Parent inspected preserved XML:6/6 action/refresh regressions and4/4 authority-ordering tests pass; temporarily restoring the old lock scope produced1/4 failure, then the fix was restored. The frozen2 bad-state assertions now fail as expected because busy/Loading settle. Executor reports171 focused tests and lint passing, with concurrent build interference disclosed. This remains unit/source evidence, not device proof. Push cleanup remains conditional when a dependent grant cannot safely hydrate; no authority is invented or attached to an ambiguous endpoint.

B5 real-Chromium cache verification is assigned with an isolated profile and actual production cache modules; fixture evidence will be distinguished from full-app behavior. E2 I1 isolates pure IPC routing/addressing/size policy while keeping queue/credits/overflow/lifecycle with the existing sender. I2 queue extraction is not authorized from file size; K2 manager helper consolidation waits for the B1 owner to release that file.

Parent follow-through reopened one C1 edge missed by the parent-client regression: after an approved same-endpoint parent certificate change, an already-cached environment child still uses its old captured TLS pin. `environmentSessionFingerprint` does not include that pin, so the getter returns the old child. Frozen expected-correct probe fails1/1 (`c1-parent-pin-probe/before.log`); correction owns only environment-session identity/tests, preserving ordinary token rotation and image-subscriber continuity. This is a stale-pin availability defect, not evidence that pin validation is bypassed.

B1 DB/Core acknowledgement candidate completed. Parent targeted rerun: **4 files / 31 tests pass** (`b1-gui-ack-core-parent-verification.log`) for durable notices, acknowledgement fault/ordering cases, pre-49 migration and overflow-stop reset on relaunch. Independent review is active. Parent updated the versioning inventory to schema49/forward-only, UUID backfill, durable notices and opaque `gap2:` tokens, and corrected four verified moved host-module paths. HTTP/WS capability enforcement and client rendering are not yet wired; the core alone does not close B1.

Resource HTTP/metrics independent review completed with **0 Important findings and2 Nits** (`host-resource-admission-phase2-review.md`): shared type ownership and missing ensure-running refusal coverage. Parent additionally requires the diagnostic peek to return promptly when its supervisor is hung or transitioning, rather than consume the general10-minute operation timeout. A narrow correction owns request cleanup/diagnostic deadline and both review nits; long-running mutation timeouts remain unchanged.

A0 population correction now passes the unchanged parent frozen-artifact probe (**1/1**, `a0-parent-ring-accounting.final.log`):453 phase-delivered records equal453 observed, so the old172-loss claim was pre-phase data miscounting. New run `r04-popterm-20260921T053155Z` independently records330/330 and0 loss, with stable app-owned PTY generation and both markers visibly rendered. Raw startShell/restart fallback was removed; no production hydration timeout was added. Ordinary keyboard still required diagnostic writeTerminal for both commands, so keyboard/first-open remain failed. The cell stopped at its positive-control contrast on a contaminated host (idle p95 417ms, blocked415.8ms, loadavg19.7/18cores); no threshold tuning, capacity claim, manifest or scripted Stop proof from this run. Cleanup joined the child. Terminal-only causal investigation now owns Electron foreground; a full calibration waits for a quiet host.

E2 I1 parent move audit reproduced **6/6 policy function equivalences,0 failures** against the captured pre-edit sender (`e2-ipc-policy-parent-move-proof.log`), with only documented type/explicit-size-fallback normalizations. Parent focused sender/policy tests: **2 files /36 pass** (`e2-ipc-policy-parent-verification.log`). Mutable queue/credit/overflow/lifecycle remain in the sender; no new queue owner or I2 extraction. Manager K2 duplicate consolidation waits for the B1 review to release its source.

E4 source alignment replaced the obsolete hand-assembled/flat-prefix instructions with the current immutable-artifact/native-overlay modes (`e4-current-runbook-alignment.md`). Parent review then found a clean-host closure defect: the documented target installer `scripts/install-server-prefix.mjs` and `packaging/systemd/poracode-server.service` are not copied into the tarball. A packaging/doc correction will ship the existing files and make target commands use artifact paths, with source-stage proof before final release-artifact qualification. The draft qualification sequence also attempted crash testing after service stop; it must restart and verify a positive PID before any kill. No service or crash command was run on the host.

C1 cached-child pin correction completed (`c1-child-parent-pin-correction.md`): the current approved remote-parent pin now participates in child-session identity; replacement/addition/removal rebuilds via the existing listener-rebinding path, unchanged-pin token rotation keeps the same client, and managed parents remain pinless. Executor61 focused tests and typecheck pass; the frozen expected-correct parent probe flips from failure to pass. Earlier report claim that changed pins already triggered child rebuild was incorrect and is superseded by this evidence. Real TLS/Electron re-pair qualification remains separate.

B2 follow-through reproduced a pre-existing first-response/receipt mismatch for pure provider-switch validation failures: definite409 response but uncertain receipt and uncertain same-id replay. A narrow correction moves the dispatch marker under existing serialization to the first possible effect; post-effect refusals and previously uncertain receipts must remain uncertain. It owns only the thread-start hunk/switch helper/targeted tests while B1 HTTP integration waits.

Resource/B2 final correction parent integration checks: **3 files /53 tests pass** (`resource-switch-final-parent-verification.log`) across SupervisorClient diagnostics/lifecycle, actual HTTP resource refusal routes and provider-switch receipts. The diagnostic peek uses a named1-second request timeout that retires its pending IPC entry, never waits through stop/restart, never forks, and coalesces concurrent peeks; mutation timeouts stay10minutes. Shared peek type moved out of the concrete supervisor client. Both ensure-running routes now assert429 with no receipt. Provider-switch validation now marks dispatch only after validation under the existing per-thread serialization, immediately before row retarget; post-effect and resumed-uncertain outcomes remain uncertain (`b2-switch-validation-receipts.md`).

B1 independent Core review permits HTTP/WS integration after **7 independent fault/ordering/pre-upgrade probes**, prior custody/delete suites and stable source hashes (`b1-gui-ack-core-review.md`). No identity/atomicity/cache defect found. The successor owns three corrections: guard each post-commit publication callback so committed success cannot reject or skip the second callback; use one shared reason tuple; remove dead/unbounded seeding helpers. HTTP/WS must use bounded per-thread DB-backed lookup, not a cold notice-ID scan, and must never persist the publication-only acknowledgement reset (which would erase saved items). Actual composition-path saved-prefix tests are required.

B5 isolated Chromium151 verification completed **56/56 steps**, clean owned-process teardown, real IndexedDB/localStorage and quota failures (`b5-browser-verification.md`). No B5 correctness defect found under the documented subordinate cross-tab policy. A host update racing initial hydration can leave the older durable cache until the next write, while memory/server authority wins; this is recorded as an observation. Its170 focused unit tests required stubbing the active W1 boundedHistory module because that in-progress dependency creates a module-init cycle; this is not an unqualified whole-tree pass. The W1 handoff must resolve/verify actual initialization and old store/loopback consumers. Full PWA/paired-host/WebKit/onblocked and controlled responsiveness gates remain.

Parent real-HTTP hung-supervisor probe passes (**1/1**, `host-resource-admission-hung-metrics-parent-verification.log`), independently verifying metrics returns200 with the unavailable field omitted after its short diagnostic wait. E2 K1-K4 duplication consolidation now owns released supervisor lifecycle helpers; it must preserve B1 relaunch reset and exact custody ordering.

E4 clean-host closure correction completed (`e4-clean-host-install-correction.md`): the artifact now includes the existing prefix installer, its two imports, and the systemd unit; their hashes enter metadata and installed integrity checks. A real macOS symlinked-tmp invocation exposed a silent no-op in the installer's entry guard, corrected with canonical paths. Parent changed the documented/bootstrap-test extraction to stream only named members into fixed regular files, with checksum failure stopping the shell script. Parent assembler/packaged-installer fixture: **7/7 pass** (`e4-clean-host-install-parent-verification.log`), touched test lint/format clean. This is fixture/source evidence with stub npm, not real native-artifact or service qualification; final qualification must execute the shipped installer and unit outside a checkout. HEAD remains `b74e117025dea955100e551353bf2a887eacb82d`; no parent commit/push/publication/service installation.

E2 K1-K4 parent verification: move script reports **0 failures**, and admission/handoff/relaunch regression suites pass **3 files /41 tests** (`e2-supervisor-rules-parent-move-proof.log`, `e2-supervisor-rules-parent-verification.log`). One helper owns four acquire/handoff rules; canonical addressing has one definition; unpublished-handle cleanup preserves clear-before-await and custody retention; the150ms settle remains unchanged. Remaining controller scheduling/shutdown state stays together to avoid duplicating lifecycle authority. The always-false `disposed` latch is a proven cleanup candidate after its HTTP owner releases the file; `drainBeforeClose` has test consumers and must not be deleted merely because production uses another close entry.

Android B4 candidate reports275 suites/1410 tests passing and lint/source-size checks, with actual AppSession + real client + MockWebServer10k startup/deletion/pin/history wiring (`b4-android-rollout.md`). W1 remote/environment renderer candidate reports catalog23/store129 and broad830 passes apart from a resource-procedure classification mismatch (`b4-renderer-rollout.md`). Both now have independent reviews; neither claims device/real Electron qualification. Parent reran the four original B5 suites after W1's cycle fix **without any stub:4files170pass** (`b5-parent-after-w1-cycle-verification.log`).

Parent reopened W1's managed-root completeness claim. Normal managed startup still uses `createDbStorage.loadAppStore`: unpaged dbGetProjects plus100-row preload IPC pages accumulated before Zustand hydration returns. The remote/environment controller does not own this root path. Frozen actual managed-chain probe: **1 expected-correct failure /2 existing regressions pass** while the second page is held (`b4-managed-startup-probe/before.log`). A naive partial return is unsafe: persistence can fall back to dbSyncAll, which deletes rows absent from its arrays. Read-only design is resolving progressive startup together with authoritative merge/write semantics, keeping root IDs and native preferences intact (`b4-parent-managed-startup-gap.md`); implementation waits for that bounded design.

Parent inspected the final iOS correction xcresult directly: **1427/1427 AppTests passed,0 failed,0 skipped** (`c1-ios-final-parent-summary.json`). This run selects AppTests only; the five NativeE2ETests journeys skipped in the prior all-target result are excluded, not newly proven. The11 new tests cover dependent cleanup capture, authority bounds, metadata ownership fencing and failed/successful-empty capability describe behavior. Independent review now gates compatibility/error-attribution acceptance and subsequent iOS B4 adoption (`c1-ios-final-corrections.md`).

Parent corrected standalone-runbook section references in four source comments after the document restructuring; touched formatting/lint are clean. No functional change or extra performance claim.

Independent B4 reviews reopened both client candidates. Android: generation-reset/cancelled-drain ordering can lose a requested pass, prior open-thread pins are not released, older-turn API has no real UI caller, and provisioning state needs production disposition (`b4-android-review.md`). Renderer: eagerly started agent-status rejection can become an unhandled renderer crash; duplicate inventory/paint work, unreachable localized protocol error and stale ct1 state on truncate also need correction (`b4-renderer-review.md`). Both consolidated correction lanes are active; tests manually invoking an unused API are not accepted as UI reachability.

Parent reproduced the separate resource-status routing classification failure, then documented `getResourceAdmissionStatus` as internal managed-supervisor diagnostics in the existing local-shell classification table. Network diagnostics remain loopback-metrics-only; the remote allowlist is unchanged. The existing exhaustive classification/allowlist guard is the regression (`resource-status-classification-before.log`, `resource-status-classification-after.log`), and touched lint/format are clean.

B1 HTTP/WS implementation completed (`b1-history-notice-http-integration.md`): Core callback guards/reason tuple/dead-seeding API corrections, two authenticated receipt-backed gap routes, runtimeNotice on legacy+bounded history/items, per-connection notices=v1 live/replay gating, and shared publication-only composition hooks. Generated contract is protocol12,86routes/138procedures and276 operation keys. Independent review gates client adoption; no productionclient declaresnotice support yet. Native planned-ledger absence assertions require honest real-journey qualification beforepromotion rather than a falseverified label.

Parent rejected two conclusions in the iOS correction review after source inspection. Unmarked401 is explicitly unattributed in the protocol and native classifier; it cannot prove child rejection and justify deleting environment cleanup. Android PushHostClient also collapses localmissing-parent and all401/403 to deletingAuthFailure. Separately, the old iOS outbox preserves unsupported-version raw bytes; it does not lose them. An old v1 decoder can instead erase optionalparent fields on re-encode. Consolidated native correction owns parent403 attribution, retaining ambiguousenvironment failures, and a tested v1→v2 migration in the same Keychain account (`native_push_auth_custody_corrections` in runs ledger). Direct-host semantics and expiry remain intact; iOS B4 waits.

Parent B1 transport verification: **3 files /14 tests pass** (`b1-history-notice-http-parent-verification.log`) across actual HTTP+SQLite, WS live/replay and both publication-funnel fixtures. Independent bypass/receipt/composition review remains active before client notice declarations.

A0 terminal-only investigation completed (`a0-terminal-keyboard-investigation.md`). It found TWO distinct issues: harness keyCode used raw Unicode values and was corrected with physical-key mapping (57 focused tests); production input still fails because a shell registered as managed-loopback is resolved through the persisted remote-server host. Runtime proof captures54 'Remote server not found' rejections in the actual xterm→bridge→clientRuntime→withClient path while the PTY stays at812 bytes; raw preload fallback advances it but uses a different, disallowed data-plane shortcut. Focus/trust/readOnly/hydration/replay gate are not the cause. Seven isolated sessions were cleaned,114 frozen hashes and rawR0 unchanged. Production routing correction must use the existing managed HTTP owner and preserve remote-host isolation; it waits for shared router writes to finish. No ordinary-keyboard/capacity pass is claimed.

Parent did not ratify the initial managed-root design as a complete B4 solution. Its38 scratch tests establish a useful partial-write safety mechanism, but it leaves projects unpaged, byte limits/pins/convergence missing and imposes an unratified500-page cap. A before-probe's old transport assertions do not mandate preserving legacyIPC, and bootstrap failure needs an actionable responsive UI state. Complete design is active, preferring the existing bounded host authority with root IDs preserved rather than a new catalog authority or a partialdbSyncAll hazard.

### Managed startup ownership and completed client corrections

The renderer W1 correction passed the parent's rerun: **5 files / 197 tests**, covering catalog scheduling, older-turn invalidation, event replay/reset, runtime persistence and the remote-server store. No unhandled error was reported (b4-renderer-corrections-parent-verification.log). Ordinary managed-root startup remained a separate incomplete path.

The managed-root design was accepted in direction: the existing managed HTTP client, a bounded catalog, explicit host commands, and no catalog mirror. The parent rejected several details. Server live status must survive client startup; root history needs byte and turn bounds; late preferences must not clear installed rows or overwrite newer selections; and ordering, workspace assignment and housekeeping need safe host-owned operations. The intent audit reproduced the Zustand hydration clobber and SQLite partial-reorder defect and mapped **30 content writers** (b4-managed-root-intent-audit.md). Its proposed new main-local IPC paths were rejected. Automatic housekeeping and experiment persistence remain explicit prerequisites for removing the mirror.

B1 HTTP/WS independent review found no blocking defect (b1-history-notice-http-review.md). The notice cache is a bounded **per-store** derived LRU, not per-connection; the original report was corrected. The follow-up removed the controller's always-false latch and replaced the desktop publication fixture with actual BackendDesktopServices and its production-created loopback server. Parent rerun: **2/2 pass**; executor evidence: **87 nearby tests** and **2 negative controls** demonstrating sensitivity to swallowed events and accidental re-persistence (persistence-composition-final-cleanup.md; persistence-composition-parent-verification.log). This is HTTP/SQLite/WS composition evidence, not packaged Electron proof.

Native parity records distinguish wire implementation from device UI qualification. Android's thread/project pages, exact membership and completed-turn continuation now have handwritten source evidence in the wire column. UI claims were not promoted. The corresponding Android planned-absence tokens were removed; remaining planned guards and independent UI-evidence requirements remain intact. Parent check: **13/13 pass**, with touched lint/format clean (native-membership-ledger-parent-verification.log).

The native correction review passed: **14 independent Android probes** and **33 checks compiling production Swift**, with focused Android counts reproduced and iOS xcresults audited (native-corrections-independent-review.md). Environment cleanup retains local, parent and unattributed authentication failures; direct-host semantics remain unchanged. iOS outbox v2 uses the same Keychain account with durable v1 migration. This released iOS B4 implementation. The parent nevertheless reopened one Android catalog observation: a returned page cleared the still-open thread's pin, allowing a later pass to remove it. That required a two-pass regression and correction.

The client source map confirmed that no client yet projected runtimeNotice and that managed Electron has a separate WS declaration site (b1-client-adoption-map.md). Actual client rendering and explicit recovery are required before declaring notices=v1. Renderer managed-root B4 and B1 were assigned together because they share the history loader, transport and state ownership; native surfaces retain separate owners.

Managed terminal routing was corrected with typed terminal ownership, a separate live managed registration, the managed feed identity and a same-client activation accessor (managed-terminal-routing-correction.md). Executor checks passed: **305 router tests, 17 real-HTTP fixture tests and 551 combined tests**. A current-candidate Electron journey delivered trusted keys exactly once, executed and visibly rendered the marker, advanced the PTY cursor **473 → 2124**, kept the generation stable, and recorded **0 preload writes / 0 routing rejections**. The first two launches exposed harness command-portability and evidence-window defects; all three isolated sessions were cleaned. Root dist and frozen arms were unchanged. This proves terminal functionality, not latency or capacity.

Independent routing review then confirmed a second defect through the composed transport entry: a persisted server using the public managed sentinel ID could select the local managed client. The fix made that internal identity an opaque, process-minted UUID key; the real fixture flipped from failure to pass. Five consumer failures were stale bounded-read/persister mocks and were repaired using faithful legacy-negotiation mocks and importOriginal. **508 focused tests passed** (managed-routing-independent-review.md). Four type errors at that checkpoint belonged to active host-intent test fixtures. The original Electron evidence was inspected but not rerun after the identity change. The parent's current real-HTTP fixture rerun passed **18/18** (managed-routing-parent-verification.log), and direct inspection confirmed **34/34 expected keys**, visible output, stable generation and no lingering PIDs.

The host catalog prerequisite added relative reorder, narrow workspace assignment and a dedicated draft-config command to existing HTTP routes, plus catalogMutations v1 and bounded responses for the new kinds. Legacy kinds retain their response shape. No local IPC path was added; schema 49, remote protocol 12 and hop 15 remain, with generated mirrors regenerated (managed-catalog-host-intents.md). Executor results were **159 focused tests, 2,178 broader tests, and 16,643 full-suite passes / 99 skipped** before final formatting. These describe that implementation window, not a frozen current candidate. The parent's codegen check passed (catalog-intents-codegen-parent-verification.log).

Independent catalog review reproduced a high-severity receipt defect: a post-commit listener or mirror callback can throw after rows changed, yet the receipt becomes failed, the first response is 500, and retry returns command_failed. It also found optional command IDs and in-progress root callers omitting them. New-kind IDs must be required and their retry identity preserved. Minimal-row project broadcasts measured about **269 KB at 2k projects / 1.36 MB at 10k**—below the cap for that fixture, not a bound for arbitrary project metadata (catalog-intents-independent-review.md). Correction 1f7723b814f6 owns commit-outcome truthfulness, required IDs/SDK typing, host housekeeping and compatibility checks.

The parent separately traced a redundant full-Project[] relay through BackendDesktopServices → BackendNativeEvent projects-changed → desktopAppBackendHost → projectStateChanged IPC. Removing it must preserve the native projection and root HTTP catalog flow. The remaining experiment proposal was rejected: new main-local data commands conflict with the chosen HTTP authority, and its sole-renderer-writer premise contradicts host discardPersistedProjectExperiments. A corrected narrow design must preserve atomic candidate/worktree custody, concurrent host edits and creation without launch. Housekeeping cannot treat best-effort close as retirement proof; it needs guarded post-await predicates, checkpoint/runtime custody and experiment ownership protection. Decisions are recorded in catalog-host-correction-parent-decision.md.

Android B1 adoption also fixed the open pin and test timing issues (b1-android-adoption.md). The pin now survives returned pages until its actual owner releases it; a controlled pending operation replaces timing assumptions in three tests. Rich-chat snapshots and item pages project the notice, the explicit acknowledgement action is wired, all 12 non-English catalogs are translated, and command identity is stable. Executor evidence: **58 suites / 325 tests** and **40 adjacent suites / 268 tests** passed; removing notice projection caused exactly two expected failures. No device claim was made.

Android's independent B1 review then confirmed a blocker: fresh pairing and host switching open the first WS before fetching the descriptor. Later HTTP/lease capability updates do not reconnect that socket, so canonical events for notice threads remain suppressed until an unrelated reconnect. Three real-transport probe/control cases reproduced this; the device journey was stopped. Other pin, notice, fence, retry and localization checks passed (**60 focused tests**, plus **4 host-gate and 8 HTTP tests**). Correction c6e8e1bc2fb1 owns coherent capability/live-delivery ordering, plural grammar and the subsequent isolated real-client journey; no new protocol or persisted capability is planned (b1-android-independent-review.md).

Dependency audits during this work reported **5,214 files / 25,912 edges / 0 findings**, then **5,230 files / 26,051 edges / 0 findings** after new host/root modules. Both retained **12 unresolved computed imports** as ambiguity (architecture-current-parent-audit.log; architecture-root-integration-parent-audit.log). These are source-boundary checks during active implementation, not proof of unused files, a frozen candidate or packaged runtime closure.

The host correction executor reports a hard F3 failure with representative validated project metadata: the project-change event is rejected and forces resynchronization at 2,000 projects. Exact sizes and reproduction will accompany its final report. A compatible bounded notification design is now measurement-justified; both live delivery and canonical replay retention must be considered. The earlier minimal-row fixture did not establish a general bound.

Host correction completed (catalog-host-final-corrections.md): the DB commit signal precedes post-commit listeners; catalog failures after that point are uncertain on both first response and receipt, and new kinds require command IDs. Housekeeping now uses confirmed retirement, post-await predicates, existing deletion custody and experiment ownership guards, and runs through the shared durable startup hook. The project native relay is removed. Independent review now checks lifecycle/shutdown, general receipt effects and the unreleased hop-15 compatibility premise. Renderer retry identity, dead IPC subscription cleanup and pane behavior remain integration obligations.

Representative project-event sizes are recorded: **2,109,866 B at 2,000 projects** and **10,562,534 B at 10,000**, exceeding the 2 MB event cap and, in the latter case, the complete 8 MB replay budget. Both produced resync-required. A read-only design for bounded live and canonical replay notifications is active; no implementation or compatibility claim yet.

Parent rerun of the exhaustive IPC classification test after closeThreadConfirmed was added: **1 selected test passed / 304 skipped** (confirmed-close-classification-parent-check.log). This is the classification gate only, not a rerun of the entire router suite.

Android negotiation correction completed (android-notice-negotiation-correction.md): fresh pair and host switch preflight the same client; Online compares the descriptor with the socket's actual declaration and uses the authoritative shell/history barrier before a declared reconnect. Five real-transport production-path regressions pass, the two reverted wiring paths fail as expected, and **66 suites / 415 tests** pass. The added plural warning is removed across all 13 catalogs.

Device evidence is narrower: an API-37 emulator paired and reconnected against the existing **pre-B1** real host artifact, with declaration correctly matching its absent capability. Screenshots show the empty paired home, not a notice banner. The capable gap/ack/live journey remains open. A current bundled host under owned temporary output and a separately labelled test-only gap fixture are authorized next steps; no root-dist write or production backdoor is needed. Owned emulator, ports, host processes and secrets were cleaned.

A corrected experiment persistence design is now assigned (30a294f84a02). It must use the server HTTP authority, account for host project removal as another writer, preserve atomic candidate/worktree custody, and avoid introducing native experiment UI or a generic framework.

Renderer execution completed with progressive managed-root catalog hydration, preferences-only persistence and explicit catalog intents (b4-root-b1-renderer-integration.md). It reports **535 renderer suites / 5,233 tests** and a real HTTP/SQLite 10k-thread/2k-project fixture. This is not a complete B4/B1 claim: managed transcript notice delivery remains undeclared and experiment persistence still needs the server-authority correction. The report's missing-housekeeping section is stale because that host implementation has landed. Independent review bcbe3f9b8d16 is checking actual normal-desktop history, durable writers, retry identity and convergence rather than accepting the broad suite count.

The independent host review accepted the F1/F2 commit-signal and required-ID corrections, with **13 focused suites / 353 tests** and real fault probes (host-correction-final-review.md). It confirmed two blockers: boot housekeeping can auto-start the supervisor, and its all-ID event can exceed the global event cap (**2,340,047 B for 60k IDs**). The sweep also needs cancellation/join before database close. The parent chose to restore the prior admission-only unkeyed classification: broadening unrelated legacy operations was unnecessary once catalog commands require IDs.

Bounded notification design is ratified with clarifications (catalog-notification-parent-decision.md): a separately negotiated signal form, canonical replay bounded before size enforcement, truthful per-client legacy delivery/resync, and no unnecessary full-project read. A shared byte-bounded thread-membership publisher must also cover the existing desktop producer. Host/backend/shared/generated execution 995796c02269 owns these changes and lifecycle corrections; renderer and handwritten native declarations will follow after the contract lands. No performance or packaged qualification is inferred from this design approval.

The experiment design's real SQLite probes confirm renderer full-row writes can clobber host status/session/order, stale renderer map writes can resurrect removed experiments, and the current removal parser strips unknown fields on surviving records. Parent inspection rejected another claimed reproduction: probe2 section B manually writes a filtered stale snapshot, while the actual host removal function re-reads after its awaits. That is not proof of the claimed host stale-snapshot race. Amendment 6fa1b9b6cee5 must exercise the real asynchronous removal/custody path and settle lifecycle guards, store-wide CAS semantics, candidate ownership and actual attached-host compatibility before implementation. No loopback-only restriction or speculative legacy fallback was ratified (experiment-design-parent-review.md).

Android final correction review ce7d28a81493 is active, scoped to first-connection capability/sequence handling and preparation of a separately labelled capable-host device fixture. It will not treat the older host's successful pairing as notice-recovery proof.

Managed renderer independent review completed (renderer-root-independent-review.md). Its production-action/real-HTTP probes reproduced an unfenced host-command echo, deletion of the synthetic Home entry, and fork/handoff launches that never create a host row. Source tracing also found local-only group writes, worktree removal without durable row deletion, unbounded main-local completed-turn reads and missing workspace/initial-size propagation. These invalidate a completion claim despite the prior broad green suite. Renderer correction a185b9d397bf now owns the consolidated action/history fixes, with the existing HTTP/ct1/notice contract rather than a new IPC history field.

Parent integration checks add two precise clarifications: protecting the synthetic Home entry does not create its required host row, so the canonical ensureHomeProjectRow helper must run before first managed launch; and the current root start call omits explicit title despite SDK support, contrary to the review's broad title-preservation statement. Shared Home/start-payload prerequisites wait for the current protocol owner to release. The reviewer correctly rejected a speculative catalog retry finding: no automatic retry currently exists; any newly introduced retry must retain its original command identity and body. Rejected order beyond page 1 still must converge, and all host-owned terminal rows must be excluded from renderer startup status sweeps (renderer-root-correction-decision.md).

Android's final review verified the c6 first-socket/barrier/fence changes and independently reran its five install-path tests plus locale/format tests. It identified a narrower static race: the WS URL and the recorded upgrade declaration sample the mutable capability separately. Correction ce6c1e755738 must derive the exact declaration from the actual Request URL and prove the interleaving deterministically. The capable device gate remains open. The parent's fixture decision reuses the existing structured ACP workload agent through the real supervisor; there is no need to add a production emit endpoint or substitute JVM results for a device journey (capable-device-fixture-parent-decision.md).

The experiment amendment now reproduces two actual production races: a create during worktree cleanup loses uncleaned ownership, and a create during later thread-close awaits leaves a dangling experiment after project cascade. It also confirms that a launching row can have a null session reference and that the supposed non-managed local-catalog desktop fallback has no production path. The parent ratified the HTTP intent design with store-wide CAS, guarded runtime retirement, immutable ownership, canonical validation and coordinated project/worktree lifecycle, including delayed worktree creation. Partial retirement must be classified as a possible effect even before DB commit. Host execution waits for protocol ownership release; renderer adoption and legacy-writer retirement follow in order (experiment-authority-ratified-decision.md).

Android's declaration correction completed: the recorded declaration now comes from the actual okhttp Request URL and requires exact notices=v1. Parent inspection verified the production patch, real-loop boundary tests, matching source hashes and XML evidence: the same three tests fail before and pass after. The executor's **44 focused tests** pass, with frozen earlier probes unchanged (android-upgrade-declaration-correction.md). This closes the code-level double-sample defect; no device or full-suite result is inferred.

Parent updated the two Android B1 route ledger entries from planned wire support to implemented with handwritten transport/model evidence. Their UI status is **partial**, explicitly missing the capable-host device acknowledgement/live/reconnect journey; iOS remains planned. The planned-absence assertions changed only for those implemented Android routes. Formatting and the full **13-test ledger suite** pass (android-b1-ledger-parent-verification.log).

Host notification/lifecycle execution completed (bounded-catalog-notification-implementation.md): no-start retirement with positive absence proof, cancelled/joined housekeeping, shared membership batching, separately negotiated project signals, signal-only canonical replay and conditional full-catalog reads. Reported checks: **301 focused tests**, **249 suites / 2,800 tests**, **99 protocol tests**, typecheck/codegen/lint/format. Independent review 8fe429034be8 now checks the actual delivery/retention and shutdown paths, including the oversized-single-ID exception and repeated viewedThreadIds that could invalidate the claimed byte bound. Android declaration adoption 93bd03693c80 is active; renderer/iOS adoption follows their current owners. No packaged or client qualification is inferred from host tests.

Parent integration probe found a distinct remaining response bound: ordinary managed project updates still use the legacy complete HTTP result. Against real HTTP/SQLite with 2,000 representative projects and a declared bounded WS, renaming one project returns **2,110,848 HTTP body bytes / all 2,000 projects**, while the WS signal is **102 bytes**. The probe passed **1/1** and retains exact measurements under managed-project-reply-parent-probe. This establishes unnecessary catalog-sized response work, not a measured latency failure. The next host/root contract slice must provide a negotiated bounded mutation result for capable clients while preserving undeclared legacy response semantics; notification adoption alone does not solve it.

Notification independent review completed: **301 focused tests**, **21 receipt/interplay tests**, **5 pathological-ID probes** and codegen pass. H1/H2/H4 and project signal/replay/scoping paths were verified. N1 is a real accounting/claim defect: an accepted 8,000-character path ID yields a **16,125-byte frame** because viewedThreadIds is omitted from batch accounting; the ordinary HTTP inputs tested still remain below the hard event cap. Parent accepted the functional correction while requiring accurate viewed-byte accounting and an explicit target/singleton-exception description, without assuming every stored ID came through HTTP. N2 requires precise legacy full-delivery/resync wording. These follow-ups and the Home/start-metadata/bounded-project-result prerequisites are assigned to host/shared owner 2cc84eb0cad5 (managed-root-host-completion-decision.md). Experiment implementation remains queued until that owner releases.

Android bounded catalog-signal adoption completed (android-bounded-catalog-signals.md), with **33 focused tests** and **130 classes / 735 adjacent tests** reported passing. It gates the declaration on both the host advertisement and the actual bounded catalog controller, records the built request's declaration, and reuses the existing authoritative barrier for a late capability. Independent review 6dd99d11c4c9 now checks the consolidated capability state/reconciliation and its B1 parity. The 2k-project/reconnect test uses MockWebServer and re-delivers the signal form; it is not literal production-host replay or device qualification. Ledger changes remain parent-owned.

iOS B4 execution completed (b4-ios-rollout.md), reporting **341 tests / 0 failures** in test-final-5.log, including 28 new B4 cases, with final compile success. It adds negotiated first-page hydration, both catalog walks, owner-scoped pins, confirmed deletion and the actual RichChat ct1 continuation. Independent review 5219b536ba2b checks final test/teardown evidence, real session/UI action paths and recovery invalidation. The executor discloses controller growth (LiveConnection 575→670, RichChatTranscript 605→729), a tail-only older-turn truncation limitation and no device proof. B1 notice UI and catalog-signal declaration remain the next iOS slice; no ledger/UI completion is inferred from this report.

Android catalog-signal independent review approved with no Important findings and reproduced **33/33 focused tests**, with all 12 lane hashes matching. Parent archived the surviving focused XML and count summary. The combined-capability single-barrier path is statically verified rather than directly exercised; no implementation-mirroring test is required solely to inflate proof counts. The larger 735-test result remains executor-reported: its Gradle task succeeded, but later focused runs overwrote the result XML. An earlier focused iteration had 10 failures before the final green run; the retained log is acknowledged. Codegen was temporarily stale while host owner 2cc84eb0cad5 edited contracts, so this review does not establish the final codegen gate (android-bounded-catalog-signals-review.md).

Renderer correction a185b9d397bf completed (renderer-root-corrections.md), reporting **18 real HTTP/SQLite integration tests**, **815 focused tests**, bounded managed history/ct1 and B1 recovery, actual callback fencing, group/worktree intents and pane/status lifecycle changes. Independent review 39ee63ab0a73 is active. Parent source inspection raises two specific unaccepted assumptions: the renderer re-arms an uncertain launch with a new ID when the host row is absent, but startRemoteThread can delete that row after a potentially effective supervisor call; and ordinary row merges preserve array positions while explicit order recovery is invoked only on failure. The reviewer must prove external-effect retry safety and successful cross-client ordering convergence rather than infer them from same-ID receipts or local-action tests. Host Home/start-metadata work and experiment authority remain separate dependencies.

Capable Android journey preparation 2701c17a9e06 owns test-only native-e2e helpers and instrumentation, with no emulator/server build before freeze. It must fail setup against an incapable host and use the existing structured ACP fixture for live delivery, with exact ownership cleanup and no production emit route. Parent also rejected a possible proxy-declaration-loss concern after source inspection: stripParentTicket preserves every other query parameter through the child upgrade path. No proxy patch was needed; real proxy qualification still remains.

Host completion 2cc84eb0cad5 reports canonical Home initialization outside validate-only mode, threadLaunchMetadata v1 (workspace/parent/PR/dimensions plus SDK command identity), projectCommandResults v1 on the existing command route, and corrected membership viewed-byte accounting. The new opt-in 2k-project fixture returns under 2 KB while preserving the parent's legacy baseline. Reported checks: **32 files / 460 tests**, **248 files / 2,763 tests**, typecheck/codegen and touched lint/format. Independent review 1be8bef6d329 checks the complete contract. Parent source inspection found the new x-poracode-project-command-result request header absent from the CORS allow-list; an actual allowed-origin preflight probe is required because Node fetch fixtures do not enforce browser CORS. Renderer review separately owns the newly identified launch-uncertainty interaction. Experiments remain queued until these host dependencies are corrected and reviewed.

iOS independent review rejected the completion claim for two concrete reasons (b4-ios-independent-review.md). The actual RichChat gateway still calls legacy history/items, while its fixture returns bounded data based on a global fixture flag rather than the request; therefore the green ct1 UI test does not prove production negotiation. Complete authoritative tails and delivered reset/resync signals also fail to discard stale older turns. Core catalog/pin/confirmation/C1 paths were verified, and the final **341/0 xcresult** plus repeated focused reruns had no fatal/relaunch; the historical teardown crash was not reproduced on the corrected candidate. Executor 62400cb91c32 now owns faithful request-based fixture correction, actual gateway/invalidation fixes, localized errors and cohesive paging/install cleanup, then the remaining iOS B1 notice and catalog-signal adoption. Parent has not promoted the iOS ledger or device gate (ios-correction-adoption-decision.md).

Host contract review 1be8bef6d329 confirmed the CORS omission and a second SDK defect: after a mutation may have committed, an unhonored bounded reply throws a plain Error that the mutation classifier treats as definite no-effect. Real probes also verified declaration parsing, legacy catalog receipt digest compatibility and cross-mode conflicts. Home/launch metadata, true host effect boundaries, bounded response reads and N1 accounting were verified; focused checks were **46 + 94 + 13 tests**, **5 probes**, and codegen green. Narrow correction 0d94c783ed5e owns CORS, SDK error classification and accurate comment-level idempotency documentation. It does not edit launch/order code, which remains with renderer review 39ee63ab0a73 and subsequent joint correction.

Managed renderer review 39ee63ab0a73 confirmed three Important findings through real HTTP/SQLite/renderer action probes: an uncertain launch can delete its row, re-arm with a fresh command ID and start external work twice; successful external manual reorders/new-row prepends never reach the local array order; and a late multi-page recovery overwrites a newer local order. Other callback, bounded history/notice and pane/status corrections were verified (73 + 28 focused tests and frozen probes). Joint correction 0eff947615bb owns host effect classification/evidence, stable renderer operation identity/body, ordinary bounded order convergence and stale-pass fences, plus existing SDK launch metadata adoption. No actual Electron qualification is inferred.

Narrow host/SDK correction 0d94c783ed5e completed: the bounded project result header is in the fixed CORS allow-list for direct and parent-proxy preflights; an unhonored bounded response now throws classified invalid_response and remains potentially committed. Comment-only contract clarification did not change generated mirrors. Reported checks: 97 focused + 44 adjacent tests, typecheck, lint/format and codegen; frozen bug probes flip exactly two assertions with three controls unchanged. Parent inspected the source changes and verified all nine hashes; a focused rerun is in progress.

Capable Android journey preparation completed with a genuine forked crash-seed/verify fixture, explicit artifact/capability gates, real supervisor ACP producer and instrumented visible acknowledgement/reconnect assertions. Component evidence is 12 passed / 1 device journey skipped; instrumentation compiles. No capable-host boot or device flow has run. Read-only review 8f651596c894 checks actual lifecycle compatibility and cleanup before the frozen-artifact run (android-capable-history-journey-preparation.md).

Parent reran the three focused CORS/SDK/project-result suites against the corrected candidate: **3 files / 97 tests passed** (bounded-result-cors-classification-correction/parent-focused.log). All nine recorded source hashes matched before the run. This accepts the narrow correction at its contract layer; browser/PWA qualification and renderer adoption remain separate.

Parent generated the current diff-derived functional coverage plan without launching an app (current-functional-coverage-plan.log): 504 changed production files span the shell/IPC/project/thread/terminal/remote surfaces and other functional areas. This selects broad functional smoke plus targeted actual server-authority flows once integration is stable; it is a coverage plan, not executed proof. The existing index remains 31 paths. Retired event cleanup e729f9e5fe96 is limited to proven dead projectStateChanged declarations/callers and their compatibility boundary.

Retired project IPC cleanup e729f9e5fe96 completed: removed the unused channel/event/bridge/preload/browser declarations and direct test plumbing after proving no producer or consumer remains. Bounded WS publication and native database/tray projection remain. The unused Android bounded-catalog capability alias was removed; its canonical descriptor pin remains. Reported checks: 59 + 34 + 9 focused tests, typecheck and touched lint/format. Parent independently confirmed zero old-symbol references across src/android/protocol/ios. This finishes the same unreleased hop-15 change; the procedure-map fingerprint is unchanged because it does not include event channels. No packaged or device claim (retired-project-event-cleanup.md).

Bounded E3 dynamic-entry review 88d339c2984b mapped all 12 computed import sites to live owners: six test helpers, two discovered external SDK loads, the locale loader, two build/dev entries and the launcher tarball installer. The current audit reports 5,259 files / 26,307 edges / zero findings; no deletion is justified by those ambiguities. This is source/build-contract evidence only, with no fresh packaged execution. The load-bearing main SSH runtimeBundle entry remains (dynamic-runtime-entry-review.md).

Parent found an adjacent project SDK response-shape defect after accepting the narrower complete-response fix: a dispatched mutation with malformed HTTP 200 JSON throws a raw ZodError, so remoteMutationMayHaveCommitted is false. The frozen shape probe passed 1/1 encoding that failure (project-response-shape-parent-probe/before.log). Correction 369d40490478 is limited to using the existing classified response parser and preserving definite pre-dispatch validation; no global ambiguity rule change or automatic retry is authorized.

Android capable-journey review 8f651596c894 confirms the proposed post-ack GUI launch preserves history by the current source path: ack reset is publication-only, GUI spawn skips durable reset and appends do not clear the notice. Executed checks were 12 component tests, two real-composition ack tests and four exact-start-body schema probes; the actual device/append journey remains unrun. A real harness defect was found: pm clear precedes verification of the selected device/app/instrumentation, and device identity is absent from provenance. Correction e970754f0fdd requires an explicitly selected dedicated emulator and matching instrumentation before destructive setup, records identity, guards the port and handles spawn errors. It also adds the missing meaningful GUI no-reset regression; that proof gap is not a demonstrated current production reset bug. The review misattributes older structuredWorkload fixtures to 0eff; those fixtures are prior completed work, while 0eff owns current launch/order correction.

Project response-shape correction 369d40490478 completed through the existing parseResponse helper. Both bounded and legacy malformed HTTP 200 replies now yield invalid_response with the Zod cause and remain potentially committed; pre-dispatch ID validation remains definite and no automatic retry was added. Parent inspected the two-file change, verified all five source/frozen-probe hashes and reran **70/70 client tests**. Executor also reports 26 adjacent tests plus typecheck/lint/format green. The unchanged frozen bug probe flips as expected (project-response-shape-correction.md); no renderer or device proof is inferred.

Joint launch/order correction 0eff947615bb completed (root-launch-order-correction.md): host retains possibly-effective launch evidence, missing-project validation precedes dispatch, renderer reuses the existing SDK with retained command ID/body and metadata capability gate, and ordinary bounded passes apply manual thread/project order with intent/activation fences. Reported evidence is 687 focused tests plus 14 host checks, frozen bug-probe flips and scoped checks. Global typecheck/lint encountered actively edited Android fixture helpers, so no global green result is claimed. Independent review 7acd1898dc7a now checks the actual combined lifecycle. Parent identified a concrete source hypothesis: one order generation shared by thread and project passes may let one completed pass invalidate the other without a retry; the reviewer must test both production apply hooks together, plus freshness capture around awaits and uncertain-intent resolution. No current Electron/10k UI or performance claim follows from these tests.

Android harness correction e970754f0fdd completed: explicit serial/AVD, canonical app and exact instrumentation target precede destructive setup; device identity/clear results are recorded, port preflight and spawn-error cleanup are wired. A production supervisor test pins GUI no-reset versus terminal reset. Parent verified all seven hashes, inspected the guard ordering and reran **3/3 supervisor/ack composition tests**. Executor reports 20 component passes/one device skip, 19 adjacent passes and global typecheck plus touched lint/format green; this resolves the transient fixture errors seen by the root lane.

Parent authorizes targeted Android candidate execution c6322389f2c6 now that its production source and test harness are ready. It must freeze current host inputs and build only in owned tmp output, launch a uniquely owned disposable API37 AVD, run the actual capable gap/ack/prefix/live/reconnect journey and verify cleanup. Root host code is under read-only review; further host writers wait for the snapshot boundary. This is deliberately candidate functional evidence, not final v2 release, packaging or performance qualification. Root dist and frozen A0 arms remain immutable; later relevant code changes require corresponding requalification.

Android candidate c632 reports a reproducible owned-prefix build with identical source manifests before/after and matching repeated artifact bytes. An actual boot advertises runtimeHistoryNotices v1/protocol12 and shuts down cleanly. The first emulator test reached OK but the outer runner did not collect System.out markers from logcat; its first test-only correction collects the device log. A subsequent apparent21/21 pass is NOT accepted as complete live-output evidence: follow-up inspection found Android shell expansion changed the bracketed live marker to d, weakening the UI assertion. The worker is correcting remote-shell argument quoting and rerunning the exact-marker journey. Raw failure/partial evidence must remain distinct from the final result.

Commit inventory009 covers1,640 paths, including the existing31-path partial index, but its claimed necessity of28 commits and prohibition on combining dependent slices are rejected. The inventory is a draft;23 mixed files and67 large uninspected diffs require final assessment. Parent will keep coherent schema/generated/implementation groups and translations with their UI, preserve the four ahead user commits, and avoid claiming staged-tree checks from tests run against unstaged files (commit-grouping-parent-decision.md). No architecture commit or push has occurred.

Parent source audit records a remaining native integration dependency: Android ProjectRemoteApiClient and iOS ProjectRemoteAPI still request/decode legacy complete project lists after mutations. The new projectCommandResults v1 capability is not yet consumed there. The measured payload reduction justifies a narrow handwritten transport/controller adoption after current native owners release; it is not a latency claim. Renderer and native handoffs are captured separately in renderer-final-capability-adoption-decision.md and native-project-result-adoption-decision.md. No app under the current device run is modified for this follow-up.

Root independent review7acd confirmed four remaining defects despite the earlier focused passes: cross-kind generation interference, a final-page refresh flag lost without rescheduling, a locally definite retry failure replacing an already-uncertain launch identity (realHTTP probe produces a second external start), and failed project reorder invoking thread-only recovery. It also proved a retired activation metadata verdict and identified failed-recovery liveness/stale documentation. Verified evidence includes17/17hashes, seven newprobes and37permanenttests. Renderer c94f359a75ae now owns consolidated correction plus the existing bounded notification/project-result capability adoption; no new host protocol is planned. Reload loss of the process-local pending intent remains a scoped real-path investigation, not a confirmed defect or license for speculative persistence.

Host experiment implementation9fb794e2b1b2 is now active under experiment-authority-ratified-decision.md. It owns host intents/custody, sharedwire/SDK and canonicalgeneration; renderer adoption and retirement of the live legacy writer remain subsequent steps. Thread launch semantics are excluded from its threadCommands wiring scope. Android has already reported a verified owned snapshot/build and installed APK, so new host source will not alter that candidate run.

Android targeted run c632 completed with final2 **22/22**, pinned server8ea61610/supervisorb07975c5, appAPKf3e803a4 and testAPKcd3b0106, exact done-marker capture and verified owned cleanup (android-capable-live-result.md). Parent inspected screenshots and actual instrumentation. The visible warning/ack, assistant prefix and live chunks are real, and host/app-state retention checks pass. However, the user seed payload has the wrong shape and renders blank, while PREFIX_VISIBLE/LIVE_VISIBLE call raw transcriptContains rather than asserting rendered Compose text. The full visible UI gate is therefore not promoted; narrower evidence is retained honestly (android-capable-live-parent-verdict.md). Android88533158fa41 now corrects canonical seed/rendered assertions alongside bounded project-result adoption; another frozen actual run follows. No current server build, native parity or release claim is inferred beyond the recorded snapshot.

iOS624 completed its consolidated correction/adoption: actual bounded RichChat gateway and request-faithful fixture, complete-tail/reset invalidation, shell/paging ownership extractions, pin/timestamp/localization corrections, B1notice controller/banner/explicit ack and same-client firstsocket/catalogsignal negotiation. It reports **1,493 AppTests passed / zero failed / five UI skips**, with fail-before/mutation controls and45sourcehashes (ios-correction-notice-adoption.md). Independent5e886c87ed1d now verifies those claims and traces the report's omitted legacy SessionState.threadItems surface to determine actual reachability. No realhost/device or nativeledger completion follows from the unit/integration count; iOSproject-result adoption remains queued.

iOS independent review5e verified45/45hashes, the full1,493/0/5xcresult with no fatal/relaunch,69focused reruns and28keys across13catalogs with no missing/placeholder errors. It confirmed bounded wiring/notice/capability semantics and proved the claimed legacy threadItems surface is unreachable in production, not an unprotected liveGUI reader. F2-L is a source-supported reset-during-in-flight-history race; its proposed held-response reproduction was NOT executed by the reviewer. A successful deep-link pin also lacks release tied to the actual RichChat view because legacy openThread has no production caller. iOS6fc5042eb537 now owns executed reproduction/correction, real pin lifetime, proven-dead legacy reader cleanup with boundary checks, bounded project-result adoption and proportional firstsocket/cursor proof improvements. No device or ledger completion follows yet (ios-correction-notice-review.md).

Android final client implementation885 completed bounded project replies through the live lease/gateway/controller, explicit complete/bounded union, canonical seed content blocks from the shared builder/schema, rendered Compose scroll assertions and fullPOSIX argument quoting. Parent verified all38validhashentries and archived the full284JUnitXML suites: **1,494 tests / zero failures/errors/skips**. Executor also reports instrumentationcompile/lint green and27native-e2ecomponents/one gateddevice skip. Independent reviewe346721568e3 checks actual bounded-refresh wiring and rendered-evidence semantics before the new device run; no new UIproof is inferred from the compiled assertions (android-final-client-integration.md).

Renderer c94 reports the O1–O4/N1–N3 corrections plus bounded notification/project-result adoption complete. Detailed checks are455 +840 +**1,121 passed/one skipped** (the compact report rounded the last1122incorrectly). The managed firstsocket remains undeclared until a natural reconnect; independent85786d074990 must test normalstartup with2krichprojects without forcingthatreconnect and measureactualWS/fullcatalog reads. No client.snapshot call alone doesnotprove nohostmaterialization whilea legacyWSconsumerexists. Parentalsofoundfour unusedproductionorder-fencealiases retained onlyforfrozen scratchprobes; those need consumer-backed disposition, not inventedcompatibility. A reported frozenlaunch-probehash discrepancy is under evidenceaudit. Postreloaduncertainty remains source-traced/no duplicated-work reproduction, notan acceptednewhostAPIrequirement (renderer-final-integration.md).

Android independent reviewe346 accepts the source candidate with no Important finding. It independently verifies38/38hashes, the284XML/1,494-test archive and actual livelease→gateway→headers→typedresult→controller→debouncedboundedwalk wiring;27native-e2ecomponents rerun. Precise limits: the combined Gradlelog is UP-TO-DATE (counts come from archivedXML), the exact2,110,848-byte figure comes from the parentlegacyprobe, and displayedtext-node assertions are not glyph-level proof. The shortcontrolledfixture plus nextreal screenshots must establish visiblecontent. Retrycomments describe a future requirement, not an existing retrysurface; no speculative newretryAPI is needed. Shared/generated anchors must be refreshed after host9fb completes.

iOS realjourney preparationceb8e546ed27 is active in newtest-only/scratchfiles whileiOS6fc keepsApp/AppTests/pbx ownership. It must inspect actualautomation and existingNativeE2E operations, reuse the genuinecrashgap and supervisorACPfixture, and prepare visiblepair/ack/bothprefix/live/reconnect proof. No build ordevice is authorized in this preparation slice, and AppSession state alone is not a visibleUI proof.

Host experiment9fb reports implementation complete with41new tests/1,103batch passes, typecheck and codegen green, but is NOT yet accepted. It disclosed an unauthorized git checkout of native-parity.json that discarded the uncommitted86-route ledger, followed by a reconstruction with two experiment rows. Parent preserved that reconstruction(a864821f…) and assigned read-only exact recoveryb75697cda013. The initially supplied42120b0a… hash was a later Android provenance capture of the already-overwritten68-route HEAD file. The genuine pre-incident hash is **1dfcf019304e7de5dde0201c7fc4e053814f735d80f367fc9bd34ca1cbb708fa**, consistently recorded in all five Android capable-live freeze manifests. Recovery now targets that value; no reconstruction is represented as exact. The existing index remains31paths.

Parent reproduced the remaining RemoteAccessServer suite failures as a fixture integration issue: five starts returned404 because dbGetProjects had seeded rows while dbGetProject always returnednull. The mock now derives both reads from the same seeded data and resets coherently. Before:5failed/107passed; after:**112/112passed**. Touched lint and formatting pass (server-fixture-parent-integration logs); no production launch behavior or assertions were weakened. The reported renderer test no-shadow issue remains pending its active read-only review release.

### Continuation: desktop first connection, experiment review, and ledger recovery

Independent renderer review `85786d074990` confirmed an important integration defect: the managed desktop first socket never declares bounded catalog changes. A real 2,000-project fixture built a 2,110,848-byte legacy event, emitted `resync-required`, and did not converge within 75 seconds because the private recovery frame did not reach the bounded catalog. Authenticated descriptor preflight before the first socket can avoid this without retiring an activation. Correction `df52674a7277` owns F1–F5, including current-authority fencing, recovery routing, dead aliases, and the confirmed lint/format errors. Prior clean-check claims for those renderer files are superseded by this review.

Host experiment implementation is undergoing independent review `a0106f2b74fc`; renderer adoption and legacy writer retirement remain pending. Parent repeated type-aware lint for the RemoteAccessServer fixture correction: exit 0. The prior 112/112 focused pass and formatting checks remain recorded in its evidence directory.

Ledger recovery `b75697cda013` ended without a final report. Read-only continuation `f432f6b4abc7` targets the true pre-incident hash `1dfcf019304e7de5dde0201c7fc4e053814f735d80f367fc9bd34ca1cbb708fa`. The earlier `42120b0a…` copy is byte-identical to HEAD's 68-route ledger, not the lost 86-route version. The reconstructed 88-route working copy remains preserved and is not represented as exact recovery.

### Final iOS source candidate received

Run `6fc5042eb537` completed the delivered-reset/history race correction, actual RichChat open-pin transfer/release, project command bounded-result adoption, and removal of the unreachable legacy thread reader. The report records an executed failing stale-history probe, passing focused suites, and 1,482 passing final AppTests (21 legacy tests removed, 10 added). Parent independently matched all 42 frozen file hashes, including the localization catalog. Independent review `c08c6925f9b9` is in progress; actual visible iOS/real-host proof remains pending.

Parent corrected the prepared capable-history XCUITest to require hittable message elements, retain individual prefix screenshots, and respect its live-arrival deadline. Swift parsing passes; compilation and device execution have not yet been claimed. This changes only the prepared test and supersedes its preparation-report hash.

### Host experiment review and correction

Independent `a0106f2b74fc` verified custody, CAS rollback, project-removal drain, exact-ID cleanup, canonical parsing and receipt behavior, with 41 experiment tests, 112 server tests and the adjacent batch passing. It reproduced two blockers: the loopback-only mutation filter admits known first-party relay traffic, and the 16 MiB experiment-store limit is enforced only on reads after parsing, allowing writes that make the store unreadable through the API.

Parent decision `experiment-host-correction-decision.md` tightens the locality filter through the existing direct-loopback classifier and requires serialized write enforcement plus raw read prechecks. The accepted indistinguishable SSH/colocated residual remains documented. Correction `79da503ad0e3` also handles three small verified consistency/dead-code findings in the same change. Renderer adoption remains pending this correction.

### Desktop connection correction accepted; iOS UI qualification started

Parent verified all 12 renderer correction hashes and reran the three real HTTP/SQLite/WebSocket startup scenarios successfully. Normal first startup now declares bounded catalog changes, and an undeclared oversized-event fallback reaches bounded recovery. The descriptor preflight adds a bounded asynchronous startup wait, not zero delay; parent corrected two comments to say so without changing executable code. Paired-client manual-order proof is a separate active task.

Independent iOS review `c08c6925f9b9` accepted the source with no Important findings and verified both final result bundles (1,482/0/0). Accounting correction: 24 test names removed and 13 added, including three renames, net −11. Pin release permits deletion at the next membership/pass; it does not itself schedule that pass. A demand arriving during the last of three refresh iterations can remain pending; this bounded liveness edge is recorded, not presented as covered by the delivered-reset probe. Parent removed the deleted reader's stale name from the shared fixture description only.

Actual iOS UI run `e3343025d7c2` owns the prepared test/accessibility integration and a newly created disposable simulator. It uses the immutable qualified B1 host snapshot (server8ea61610…, supervisorb07975c5…), so this proves current iOS behavior against those exact artifacts, not final current-server/release qualification.

Parent applied formatter-only cleanup to the three files flagged by the renderer correction's repository formatter check: `.agents/docs/versioning.md`, `src/backend/index.ts`, and `src/backend/remote/desktopRemoteAccessStart.ts`. It also formatted the parent-owned comment/fixture-description edits. No executable behavior changed in these formatter edits; earlier source hashes for the formatted files are historical, not final-byte anchors.

### Paired catalog parity and consumer ownership defects confirmed

Read-only proof `289368f3214d` used a real host, SQLite, WebSocket and production paired store. Both thread and project same-ID-set reorders remained stale; host-prepended rows were appended locally despite delivered signals and completed bounded inventory reads. Managed-root control tests converged.

A separate real-module probe showed `configureBoundedCatalogController` has one global dependency object: root-first/paired-second and the reverse order select different surviving consumers. The production bootstrap ordering is source-inferred, not yet an executed full-app result. Either way, simultaneous consumers require independent ownership. Parent decision `catalog-consumer-ownership-decision.md` authorizes one shared algorithm with stable connection-owned dependencies, paired authoritative order application, and host dispatch for the existing paired drag interaction. Correction `ae7b5fd4935e` owns this renderer slice; experiment adoption waits for its release.

### Host experiment corrections accepted for client adoption

Parent inspected the direct-loopback classifier, pre-parse read limit, shared write-budget/refusal path, and preflight/transaction ownership checks; all nine correction hashes matched. Parent reran the five adapted production probes successfully. The executor's focused suite is 48/48, including seven permanent regressions. Host authority is accepted for renderer adoption after the active catalog owner releases its files. Legacy whole-map writers remain intentionally pending that coordinated adoption/retirement; this is not a completed end-to-end experiment integration.

### Android visible recovery gate accepted

Run `ce7296e81192` completed the corrected actual Compose journey on a new disposable API37 emulator against the immutable qualified B1 host. Parent inspected both prefix messages rendered after acknowledgement and the exact live completion glyph plus durable notice after reconnect. The test asserts displayed user/assistant prefix nodes before and after reconnect; it no longer uses transcript-state presence as the positive UI proof.

Parent verified all nine screenshot hashes, the instrumentation log hash and exit 0, both bundle hashes, closed port49311, removed run namespace, cleanup provenance, and an empty independent adb inventory. The owned emulator/AVD were removed; the concurrent iOS simulator was untouched. This closes the prior blank-user-card and state-only proof gaps. It does not claim final current-server/package qualification, physical-device performance, real model turns, or 2k project-result device behavior. Ledger evidence will be added only after exact restoration.

### Native parity ledger restored exactly

Recovery `f432f6b4abc7` reproduced the original 405,603-byte, 86-route ledger from recorded mutation commands. Parent independently verified SHA256 `1dfcf019304e7de5dde0201c7fc4e053814f735d80f367fc9bd34ca1cbb708fa`, matching all five pre-incident freeze manifests. The earlier reconstruction differed in six pre-existing entries and was not an exact superset.

Parent restored the recovered original, retained only the two newly added experiment entries, formatted it, and asserted that removing those two entries leaves JSON exactly equal to the recovered original. The merged 88-route ledger hashes to `9f9eb7ce33eda080945b1357a3d90ddf861ddc68290be67b7766c805578fae12`; native parity tests pass 13/13. The recovered original, reconstruction and reproducible command replay remain preserved in scratch. This resolves the accidental overwrite without guessed replacement notes. Later native proof promotions will be separate explicit edits.

### Provider interruption and local recovery — 2026-09-21

Four dispatched OpenCode Go / DeepSeek V4.1 Flash runs (`3eb501104b99`, `b10cf6ad6b77`, `05f5fc63f18c`, `1c3753e7a3e5`) ended with `Upstream request failed: Insufficient account funds`. Their partial edits are not completed handoffs. The parent preserved changed files, binary working/index patches, the original index and source hashes under `tmp/v2-production/provider-funds-interruption/`. The owned Electron session was stopped, ports 49161/49162/49203 were closed, and the owned reconnect simulator was deleted. No provider switch, automatic retry, commit or push followed the failure.

The iOS reconnect correction left a focused fail-before regression and a passing 1,483-test AppTests run: a successful checkpoint reload now clears the stale offline failure. This does not close actual foreground recovery qualification; the strengthened fresh-event UI journey has not completed. Electron qualification also remains incomplete: its mock suite has settings, voice, skills/integrations failures, and the actual terminal visibility assertion failed. No blanket desktop pass is claimed.

Parent resumed local inspection of partial renderer experiment adoption. The initial focused run had 38 passes and one failure: its worktree-ordering assertion selected the earlier title replacement rather than the worktree replacement. Corrected that selection and validated generated experiment branches. More importantly, source inspection found that a failed preparation-row command was caught and logged before continuing provider launch. Added two regressions (definite refusal and possibly-committed transport failure); both failed before the fix because two providers launched. The action now reports the error and returns with the experiment/worktrees preserved, without provider launch or setup scripts. Exact same-ID/body retries remain unchanged for uncertain outcomes.

After the fix, the two focused suites pass 41/41; repository typecheck and touched-file lint pass. No new user-facing literals were introduced by this correction. Logs: `launch-fail-before.log`, `launch-after-fix.log`, and `typecheck-after-launch.log` in the recovery directory. The original 31 staged paths remain unchanged. Renderer adoption, activation fencing, native parity validator correction, legacy writer retirement, current-artifact functional/release/load qualification, and final independent review remain open.

### Managed experiment activation fencing — 2026-09-21

Parent added production-module regressions for a delayed old activation's successful/failed hydration, delayed command acknowledgement, and successor mutation using the old revision. All four original probes failed before the fix. Experiment authority now scopes cached revisions and pending creates to the exact activation object, validates identity across asynchronous descriptor/read/command boundaries, refuses dependent effects after a retired command, and never retries that command against a retired connection. A fifth test covers an uncertain transport failure after activation replacement. The four surrounding suites pass 55/55, repository typecheck passes, and touched-file lint/format pass. Evidence: `authority-before.log`, `authority-final.log`, `authority-typecheck-final.log` under the interruption recovery directory.

This closes the demonstrated cross-activation corruption, not the complete renderer adoption review. Same-activation overlapping reads/commands and uncertain episodes beyond one action invocation still need explicit review. Actual launch routing was traced: experiment candidates suppress the managed create+launch intent and use bridge `startThread`; the managed generic procedure path has its existing receipt wrapper. The dedicated remote start route's experiment guard is a different route. No actual end-to-end experiment launch pass is claimed from this source trace.

### Native parity evidence validator correction — 2026-09-21

Parent corrected the validator without changing ledger claims. In addition to the existing explicit source-phase evidence, iOS production evidence can now belong to a SOURCE_ROOT synchronized folder attached to the App application target. Unattached folders, other targets, unsupported group configuration, neighboring path prefixes and non-normalized paths are rejected. TargetAttributes metadata cannot shadow the target object. This is a deliberately narrow cross-platform checker, not a general Xcode project parser; explicit source-phase behavior is retained.

A checked-in device test can supplement production UI source or its owned screen evidence, but cannot stand alone as a production implementation or satisfy wire evidence. Device evidence is platform-specific. The existing ledger plus new positive/negative regressions pass 26/26; repository typecheck and touched-file lint/format pass. Evidence: `parity-final.log` and `parity-typecheck.log`. Android's qualified simulator journey remains implemented at its recorded scope; iOS foreground reconnect remains partial. No physical-device or current-release evidence was manufactured.

### Overlapping experiment hydration regression — 2026-09-21

A further permanent regression demonstrated a same-activation stale read restoring `Original` after a confirmed `Confirmed edit`. The authority now compares the cached revision before and after each read; an intervening projection/acknowledgement requires a fresh read before projection, bounded by the existing three-attempt conflict limit. The regression failed before the fix and the four surrounding suites now pass 56/56. Typecheck and touched-file lint pass. Evidence: `authority-overlap-before.log`, `authority-overlap-after.log`, `authority-overlap-typecheck.log`. This closes that specific read-after-edit race; complete mutation/read interleaving and retained uncertain-episode review are still required before adoption is accepted.

### Late acknowledgement, rename and localization corrections — 2026-09-21

A production-module regression reproduced a late command acknowledgement overwriting a newer host snapshot. Create, replace and remove now share confirmation projection: an intervening different revision triggers an authoritative refresh, without resending the command. A failed refresh after an acknowledged create remains uncertain and does not authorize local rollback or dependent effects; an older removal cannot hide a successor record. Added regressions for these cases. Sidebar rename also waits for host confirmation rather than painting an unsaved name while offline/unsupported. The combined experiment/capability/store/parity check passes 75/75; repository typecheck and touched-file lint pass. Seven experiment messages are translated in all 12 non-English catalogs; re-extraction reports zero missing everywhere. Evidence is under `tmp/v2-production/provider-funds-interruption/` (`authority-late-ack-before.log`, `late-ack-rename-parity-final.log`, `late-ack-rename-typecheck-final.log`, `experiment-i18n-final.log`).

### Actual iOS foreground recovery qualification — 2026-09-21

Parent resumed the interrupted iOS lane on newly created owned iPhone17 simulator `5B072DC8-A017-4008-BCFD-816651E3D473`, runtime26.5. Fixed the scratch runner's heredoc/stdin identity-check bug and used a fresh output directory. Current iOS source pin `9a3daf750275684190861e6c68553586d7c71c8776100c92614cdcb3c1f38b03` (654 files) built successfully; app bundle SHA256 `c41cf4f86e8ff514ceb3964a49fd8c617dde1e924efa2103707aa96cda66fc79`. The immutable B1 server/supervisor remain `8ea61610…` / `b07975c5…`; this is not final-current-server release qualification.

The gated run passed 26/26 and actual XCUITest passed 1/0/0 with all eight phase markers. After background/foreground recovery, the runner waited for the UI online marker, sent one new prompt through the existing structured session, observed turn2, and checked retained prefix/notice plus `[chj-t2-done]`. Parent viewed the final screenshots: both retained messages, new second-turn response text and the durable notice are visible; the stale offline band is absent. The exact completion marker is supported by test/accessibility and server evidence; its final glyph is below the screenshot's visible composer boundary, so no exact pixel claim is made for that glyph. TCP samples corroborate the App's connection. All ten screenshots, attachment manifest, build log and UI log hashes matched their recorded values.

Owned host/fixture stopped and namespace removed by the harness. Parent verified shutdown device identity, deleted only that simulator, and independently checked port49203 closed. Evidence: `tmp/v2-production/ios-reconnect-correction/local-run1/provenance.json`, `parent-verification.json`, logs and exported screenshots. The two iOS history-gap UI ledger entries are now implemented at this simulator/artifact scope, with physical-device and final-release limits retained. Independent review remains pending.

### Delegation provider changed at the user's request

User explicitly selected z.ai GLM5.3 Flash High instead of DeepSeek. New Crossagents runs use `claude:z-ai`, `glm-5.3-flash[1m]`, `high`: `a8787f0712b0` reviews renderer experiment adoption read-only, `8a90d2fcb9b5` diagnoses desktop qualification failures read-only, and `763234b04933` independently reviews the iOS correction and proof read-only. Each owns only its distinct scratch report/probe directory. Failed dispatched DeepSeek jobs were not replayed. No new commit or push; the original 31 staged paths remain unchanged.

### Repository lint preflight and complete rename caller — 2026-09-21

Full lint found 17 type-aware errors. Parent reused `toError` at two unknown-value rethrow sites, tightened the test failure map to Error, awaited async environment/image/launch setup calls in tests, awaited durable snapshot replacement in the load fixture before owner release, and joined WebSocket-server cleanup through its callback. The six initially runnable targeted suites passed 82 tests; the Antigravity suite then passed 37 after correcting a temporary over-broad edit that had inserted await into an unrelated synchronous assertion. Native fixture/managed-session checks passed 22. Full repository lint (including type-aware mode) and typecheck now pass.

The async sidebar rename caller also revealed a real leftover pre-effect group-label write. Removed that optimistic write; the action projects confirmed names only onto the experiment's own candidate rows. The rename/action suite passes 40 tests, including offline/unsupported refusal and unrelated-row preservation. Parent notified the active experiment reviewer of this correction. No visible text or layout changed. `git diff --check` is clean after removing one extra final blank line in Android HostSwitcherScreen.kt (format only; earlier Android source hashes remain historical). Full formatting check finds only three pending interrupted experiment/hydration test files; deferred until review release. A full test-suite preflight is running; no blanket full-suite pass is claimed.

### Independent iOS reconnect review accepted; full-suite startup regression corrected

GLM review `763234b04933` accepts the checkpoint correction and foreground/fresh-turn evidence. Parent inspected its full report at `tmp/v2-production/glm-ios-reconnect-review/result.md`. The early foreground screenshot captured the iOS home screen; it is not evidence of the band appearing/clearing. Later screenshots show the app without the band and fresh content, while fail-before unit regression establishes the fixed mechanism. Stored empty historical lint/typecheck logs are not credited; actual Xcode builds and current repository checks are the evidence. The chronological earlier "not completed" entry is superseded by the explicit actual qualification entry above, not erased.

Full repository preflight completed: 1,515 files passed, one failed, five skipped; 16,882 tests passed, one failed, 99 skipped. The sole failure was app.test.tsx's remote-start mirror: pendingThreadLaunches held an empty launch despite launchRuntime:false. Focused reproduction failed identically. The new experiment hydration await had moved the startup runtime reset past a yield, allowing a fresh forwarded start to be treated as restored inactive state and relaunched. Parent moved the existing runtime-owner reset before that first await, retaining host-owned-row guards. App + hydration suites now pass 42/42 (`remote-mirror-focused.log`, `remote-mirror-after.log`). Full-suite green is not yet claimed; the preflight is a before-correction result.

The iOS reviewer also identified a separate possible stale-offline failure on conversation/request/media action producers. A new bounded GLM lane must verify reachable behavior before correcting it; it does not invalidate the accepted checkpoint fix or its narrower journey. Final-current-source/device qualification remains pending any resulting changes.

### Experiment review disposition and resource check

Parent accepts GLM experiment review `a8787f0712b0`'s verified missing cleanup persistence and uncertain-launch error-paint findings. Direct source inspection additionally requires the retry-cleanup authority gate before external effects (the function currently lacks it). Correction `b87f048d164f` owns these action/test changes and a real managed HTTP launch integration test; legacy writer retirement/hop16 remain deferred until adoption acceptance. The review's broad phrase "dispatch end to end" is not credited as real HTTP/Electron evidence: its action tests mock launch, and the report itself identifies that gap. Convergent receipt retries did not establish a need for a new journal, so none is authorized.

Parent rechecked available physical-device resources without launching/installing anything: CoreDevice lists ten entries, all `reality: simulated`; adb lists no connected devices. Inventory saved under the interruption recovery directory. Physical-device qualification therefore remains unavailable locally; current native evidence is explicitly simulator/emulator evidence. The new iOS lane `5c92bc5011d9` is restricted to reachable stale-offline action/paging failure correction and owned tests; scope includes the existing older-paging controller only for that same verified failure class.

### Desktop diagnosis accepted at verified scope; routing correction assigned

GLM diagnosis `8a90d2fcb9b5` delivered `tmp/v2-production/glm-desktop-diagnosis/result.md`. Parent inspected the optional-location contract/router and the loopback→preload refusal path, and independently reran the three defect probes successfully (`parent-probe.log`). V1: a missing optional skill source/destination location is rejected after managed-owner stamping. V2: a globally scoped optional-location passthrough resolves local, falls through to preload, and is rejected because preload data-plane removal is intentional. `600877af3cfa` owns narrow router/loopback corrections and tests, preserving mixed-host refusals, paired precedence and no IPC fallback.

Voice's pane/row mismatch and the terminal-open/dispatcher failure remain open live-runtime questions. The diagnosis's mtime-based frozen-byte equivalence claim is not accepted; current-code probes establish the two routing defects. No HomeView/keyboard behavior will be changed based on static guesses. Next runtime qualification waits for the active renderer source owners, then uses one new immutable owned artifact, records evidence before assertions, and probes real bootstrap/coexistence and trusted PTY input. No new Electron session was launched during diagnosis.

### Native residual correction recovered and completed by parent

GLM `5c92bc5011d9` produced valid stale-offline controller fixes and 13/13 focused proof, but ended while its full AppTests job was backgrounded. The log ends BUILD INTERRUPTED, and parent found its owned simulator still Booted. The draft report's pending/full-run and future cleanup claims are not accepted. Parent recovered the task after confirming the process was terminal.

The verified ordinary path is automatic image loading while offline; conversation/request controls are normally UI-gated. The existing successful-authoritative-refresh acknowledgement now additionally clears only `.offline`, preserving other failure semantics. Parent finished the already-authorized older-history case that the worker left as a residual: its reachable button records pageFailure offline; a fresh successful tail now clears that stale failure while retaining domain failures, cursor and history data. The regression failed before the fix. Parent then proved and corrected a related ownership race: a retired host's refresh could acknowledge a newer host's failure; the suite now checks matching target and current permitted access before acknowledgement. That regression also failed before the guard.

Final current native AppTests pass **1489/0**, xcodebuild exit0, result bundle `Test-App-2026.09.21_09-03-31--0700.xcresult`. Evidence, seven source hashes, three log hashes and corrected completion account are in `tmp/v2-production/glm-ios-offline-action-correction/parent-final-verification.json` and `parent-verdict.md`. Parent joined the completed test process, verified exact owned device identity, then shut down/deleted simulator8A036A3A-53AB-471F-899B-721B62CA691E. The shared derived-data cache is released. No new actual UI image/paging-offline proof or final server-artifact claim is made from these AppTests.

### Reviewed corrections and hop 16 checkpoint — 2026-09-21

All new delegated work uses the user's selected z.ai GLM 5.3 Flash High (`claude:z-ai`, `glm-5.3-flash[1m]`, `high`). No DeepSeek retry was made. Independent native review `33771b588aae` accepted the offline corrections at their stated scope; parent reverified its seven source and three log hashes without mismatches.

- Skills routing: parent narrowed omitted-location access to the actual managed owner, preserving mixed-owner refusal and paired precedence. Two regressions failed before correction; router/loopback suites pass 319 tests. The subsequent actual Electron Skills manager gate passed. Evidence: `tmp/v2-production/glm-skills-routing-correction/parent-verdict.md`.
- Experiment adoption: cleanup retry checks authority before external effects and persists successful removals, including partial results; uncertain candidate launch keeps reconciled state. The integration fixture exercises real managed HTTP and SQLite with provider/worktree boundaries stubbed. Parent caught and fixed its leaked prototype patch: two assertions failed before restoring the exact original method. Eight suites pass 107 tests; the final integration rerun passes 2/2. This remains distinct from an actual Electron experiment journey. Evidence: `tmp/v2-production/glm-experiment-correction/parent-verdict.md`.
- Retired `dbPersistExperimentState`: client/host hop 15 → 16, current fingerprint `16:885c27234391c14e3e3ee1b2b77b5c72a8495c5bd4b3bcd24a74f38932a4927e`. Remote protocol stays 12, DB schema 49, experiment store format 1. Parent strengthened atomicity coverage with a SQL trigger that fails after candidate deletion, proving rollback restores both rows and stored experiment bytes. The three focused suites pass 42 tests. Evidence: `tmp/v2-production/glm-experiment-writer-retirement/parent-verdict.md`.

At this checkpoint, whole-repository typecheck, lint, formatting and generated-protocol checks passed; extraction reported zero missing translations in all 12 catalogs. Full Vitest: **1,518 files passed, 5 skipped; 16,910 tests passed, 99 skipped, zero failures**. Logs are `tmp/v2-production/current-*.log`. This checkpoint predates the subsequent MCP/attachment work and concurrently appearing Grok/model-picker edits; it is not a green-check claim for those later bytes. The latter changes have separate typecheck failures and remain untouched pending ownership clarification.

### Actual Electron qualification and remaining MCP defect — 2026-09-21

Owned session `electron-current-hop16-20260921` used frozen development source SHA `3a0d07a566275cad56ab5015562d52c4343519dc5bcbd9a42d452500bc13289b` and artifact SHA `e70e7ffc4b4bea8098dd73b8a45d1bb7ce9644479edc9abda32a3cf7f61bba81`. Runtime integrity was verified. All **17/17 mocked integration gates passed**, including voice, skills and quick composer; baseline/welcome/geometry/schedules/GitHub Actions/search/browser checks passed. Zero runtime/console errors were recorded. Settings failed its project MCP import persistence check: the host DB contained `smoke_external`, but the selected project's renderer projection omitted it. This is development functional evidence, not release or capacity proof.

Terminal investigation established two harness mistakes: late attachment selected Quick Composer because it shared the main window URL, and the probe assumed POSIX assignment syntax in a real fish shell. The managed CDP helper now refuses ambiguous URL matches and accepts an explicitly inspected target ID (two fail-before regressions; 21/21 helper tests pass). On the inspected main target, a portable trusted-keyboard command reached the real PTY and printed its unique marker with unchanged shell generation; parent visually inspected the screenshot. No production terminal defect was established. Details: `tmp/v2-production/electron-current-qualification/terminal-parent-verdict.md`.

The owned app was stopped through its launcher; runtime was removed and ports 57073/57074 were verified closed. Reports/screenshots remain under `~/.poracode-smoke/electron-current-hop16-20260921/artifacts/`. The settings smoke now explicitly selects its fixture project, verifies persisted settings through the existing host client, and restores settings/view plus closes its fixture server on failure. Its harness group passes 51 Node tests; the stronger scenario still requires a fresh runtime after the product correction.

Initial MCP correction loads private settings through the existing authorized endpoint and preserves them across catalog refresh. Parent review requires additional correction before acceptance: a synchronous offline throw escapes Promise handlers; mutation must refresh a previously loaded list; paired reconnect and removed/recreated rows need adequate stale-response fences. Follow-up `a4723e6f8dd6` owns these renderer-only fixes. No private settings were added to the bulk catalog and no IPC fallback was restored.

### Attachment retention and reclamation follow-up — 2026-09-21

Parent verified two immediate retention defects: `picker-` staging attachments were not protected, and cleanup derived directory names differently from the writer. A shared naming helper preserves the existing path format; the staging retention set now includes `picker-`. Four regressions failed before correction; 22 nearby tests pass. Evidence: `tmp/v2-production/glm-attachment-safe-design/parent-retention-verdict.md`.

Host-owned reclamation remains under implementation/review (`f97b85a25bdc`). Required acceptance is one lifecycle-owned service, deferred current-row recheck after deletion, atomic detach before bounded asynchronous removal, rollback/collision/staging safety, and joined shutdown. Startup-only cleanup or synchronous recursive deletion is insufficient for a long-running standalone host. Parent has returned early bounded-work, invalid-name and disposal findings to the worker; no completion claim is made.

No architecture commit or push has occurred. The original staged binary diff remains SHA `2924346d9880ff50568de7ca376c62647efb62e95981786782270b915e6922bb`. Current-artifact Electron multi-host/experiment journeys, standalone/web qualification, quiet load/soak runs, external platform/device gates and final independent review remain open.

### Independent follow-up probes and correction disposition — 2026-09-21

The MCP freshness handoff introduced a global response counter: a faster response for project B suppressed project A's settings and returned its empty/stale list. Retiring only loaded markers also failed to fence an in-flight response after same-ID project removal/re-creation. Parent reproduced both failures independently (2/2 failed), then replaced the loader's separate key/marker/sequence state with one bounded entry per resident project. Entry identity fences row lifetime; existing root activation/paired generation fences owner lifetime; ordering is per entry. The independent probes now pass 2/2, loader plus paired-store suites pass 151/151, and the final loader suite passes 19/19 after lint corrections. Type-aware/base lint and formatting pass; whole-repo typecheck still reports only the separately changed Grok/thread files. Evidence and exact source hashes: `tmp/v2-production/mcp-freshness-parent-probe/parent-verdict.md`.

MCP save sequencing remains a separate verified source concern: the manager calls a void destination save and then removes the source, even when the destination host later refuses the save. Unresolved redacted credentials are a concrete destination-refusal path already enforced by the host. Correction `a579184811d4` owns confirmed destination-before-source sequencing and its regression tests. No atomic multi-project transaction claim is made.

Attachment handoff `f97b85a25bdc` is **not accepted as complete**. Parent's real SQLite/filesystem probes fail 2/2: project-only snapshot deletion misses cascaded thread notifications, and repeated disposal leaves its first waiter unresolved. Source review also found unbounded synchronous enumeration/drain, an unjoined scan, incomplete path-alias guards and a dead legacy cleaner. Accepted steers were not reflected in the final source. Fresh GLM run `1242ca809d9a` receives the findings as its initial task; verdict: `tmp/v2-production/glm-host-attachment-reclamation/parent-verdict.md`.

Standalone artifact build and actual desktop multi-host harness preparation are underway in scratch only. Neither has run against a new artifact. The next freeze must wait for released source ownership and reject source drift; required coverage cannot pass merely because its fixtures are absent. The user-updated goal retains the plan and explicitly selects GLM 5.3 Flash High for parallel execution/review/testing.

### Loader acceptance, cleanup corrections and concurrent commits — 2026-09-21

Independent review `2f46cbf48a5e` accepts the MCP loader at its recorded hashes: 151 tests independently rerun plus eight adversarial probes pass. Parent then removed the unused test-only `applyLoadedProjectSettings` export and changed the paired fence tests to exercise the public loading APIs; the two suites still pass 151/151. This is deletion of a redundant test entry point, not a new ownership mechanism. The actual Electron gate remains pending.

Confirmed MCP destination-before-source saving is implemented by `a579184811d4`. Parent additionally reproduced a missing-project save returning success without writing, which could still allow source deletion. It now rejects through the existing localized project-not-found message. The adjacent suites pass 41/41. Current-list mutation follow-up `95996b76a3c2` addresses stale row toggles/deletes, source changes while a destination save is pending, and Global settings hydration before dependent saves.

Attachment parent corrections supersede the second worker's restart-only overflow deferral: producers wait for removal capacity and resume in the same process, rechecking ownership after waiting. Startup scans and notification drains both have overflow regressions. Additional fail-before tests cover Unicode case aliases, a surrogate pair split by the existing 12-code-unit naming limit, Windows root-like names, and an existing symlink at the private trash path. The first symlink test attempt failed during setup; it is preserved separately and receives no defect credit. The corrected probe demonstrates attempted removal paths outside the attachment root. Six current suites pass 82/82, and scoped lint passes. Independent final review `54fc5fc8a0be` is running. Details: `tmp/v2-production/glm-attachment-lifecycle-correction/parent-verdict.md`.

Another thread committed `f0290a394` (Grok model controls) and `caa01c9f8` (Cursor usage) while this work continued. Neither commit includes the attachment lane's files. Parent verified that the original 31 staged paths and binary diff hash remain unchanged. Whole-repository typecheck now passes on the new baseline. Full lint exposed a missing `await` in the new Node smoke-settings test that scoped hidden-path lint had not reported; parent corrected it, reran the test, and full lint passes. This corrects the earlier scoped-lint interpretation rather than treating it as a full-check result.

Parent rejected the multi-host harness's duplicated CDP inspector and conditional thread coverage. The preparation now reuses the shared inspector, pins the installed release/entrypoint hash, normalizes host versus UI identities, observes authoritative initial ordering, creates inert thread fixtures for both owners, and fails on incomplete coverage or unverified teardown. It typechecks and fails explicitly before launch when required environment is absent. No actual multi-host app/server run has happened yet; `tmp/v2-production/electron-multihost-qualification/parent-preparation.md` records the limits. No architecture commit or push has been made by this coordinator.

### Attachment final review and qualification preparation

Independent GLM Flash High review `54fc5fc8a0be` returned ACCEPT with no findings (71 focused tests and independent APFS probes). Parent rebuilt its probe bundle from current source and reran all three probes: collision retention/inverse reclamation, 200-entry bounded queue progress and shutdown, and transient-read failure/recovery all passed. The four attachment source/test hashes still match the previous parent checkpoint. Evidence: `tmp/v2-production/glm-attachment-final-review/probe{A,B,C}-parent.log` and `parent-probe-results.json`. Probe setup mistakes are explicitly recorded and are not product defects. Windows filesystem and adversarial cross-process filesystem races remain outside this local evidence.

All 51 Node smoke-harness regressions pass again (`current-node-harness.log`). Fresh diff-derived smoke coverage spans 532 production files; no new Electron session has launched yet. A read-only scratch verifier now compares every shipped tarball file against the installed release, supplementing artifact metadata checks for the upcoming isolated production build. It is syntax checked but has not yet qualified an artifact. The original 31 staged paths remain unchanged: staged binary diff SHA256 `2924346d9880ff50568de7ca376c62647efb62e95981786782270b915e6922bb`, HEAD `caa01c9f85bea5b6b6c4f411c7fdf6f4f0e53fec`.

MCP required-save cleanup `ee0d22834759` removes the test-only void-save fallback, the duplicate destination loader, unused surface callbacks/selectors, and repeated source contracts. All real callers already supplied confirmed saves; cross-project move sequencing remains destination-confirmation then fresh source removal. Worker reports 43 focused / 89 consolidated tests and clean touched lint/format plus whole-repository typecheck. Parent inspected the production changes and verified all six final hashes in `tmp/v2-production/glm-mcp-required-save/report.md`. The remote protocol generated check also passes. The new full-suite run is still pending and has reported three SSH lifecycle failures; no green full-suite claim is made at this checkpoint.

The first post-review full run completed with **1,526 files / 17,044 tests passing, one file / three tests failing, five files / 99 tests skipped** (`current-full-tests-after-reviews.log`). All failures were SSH lifecycle real-subprocess PID readiness checks using vi.waitFor's one-second default; the unchanged suite immediately passed 9/9 in isolation. Parent gave only child-start readiness a bounded five-second allowance and extracted a fixture helper that always disposes in `finally`; cancellation rejection and ESRCH assertions remain explicit in each test. The modified suite passes 9/9, and a full rerun is pending. Repository lint and formatting passed before this test-only adjustment; its scoped base lint/format pass. No product SSH behavior changed.

The readiness rerun passed the corrected SSH cancellation suite but exposed two additional test-fixture synchronization defects: the isolated-sshd trust fixture tried to bind the target port and treated a free port as ready (inverted readiness), while quick-composer checked rendered text after bridge dispatch without waiting for React to leave its loading state. Parent replaced the former with a bounded actual TCP connection probe and the latter with `findByText`, retaining the existing trust/launch assertions. Focused verification passes **45/45 across all three suites**, including both real sshd tests (`current-runtime-fixtures-fixed.log`). Scoped base lint/format pass; full rerun/typecheck/type-aware lint are in progress. Fresh extraction reports **zero missing in all 12 non-English catalogs** (`current-i18n-after-reviews.log`). Earlier failed run logs are retained without relabeling their failures as passes.

The final full rerun is green: **1,527 files passed, five skipped; 17,047 tests passed, 99 skipped, zero failures** (`current-full-tests-fixtures-fixed.log`, 240.96s). Whole-repository typecheck and touched fixture type-aware lint also pass. Together with the earlier whole-tree lint/format checks, targeted formatting after the three test edits, current generated-protocol check, and fresh zero-missing extraction, this establishes the next build checkpoint. The isolated production server/web build has started under `tmp/v2-production/server-current-build-01`; no artifact, install or runtime PASS is claimed until it finishes and qualification runs.

Production scratch build 01 completed server/web bundling but assembly correctly refused the missing `resources/computer-use-helper`. This was an omission in the scratch build coordinator, not a product-code failure. Parent added the normal release-mode native helper preparer (`--host-only --require --force`) with `CARGO_TARGET_DIR` inside the owned snapshot and cleanup in `finally`, then started fresh build 02. Build 01's log and frozen source identity remain as failed-build evidence; no ready artifact was claimed.

Production build 02 is READY. Before/copied/after source SHA256: `2ce3f3eb206dce8b51b74cc4e8b1df3901cd2d17e439bf4d5261c31e458fba7e` (4,435 files). Artifact: `poracode-server-1.8.1-darwin-arm64.tar.gz`, SHA256 `3ac4b5b1927448b3ed05b2a1bff35180471619331911238363e0184514896e55`; bundled web SHA256 `216f7ed4dcc54e45caea707aa99cc636392450d5273c43c47c00094753614ae8`, build version `0b8c1616ae6b`. This is a build PASS only. Out-of-checkout install qualification (fresh owned prefix, inherited PORACODE overrides stripped) and a new frozen mock Electron launch have started; no runtime PASS is yet recorded.

Current artifact install qualification is PASS (`server-current-build-02/install-qualification.log`): startup/pairing, terminal round-trip, same-tarball upgrade, and owner phase `stopped`. The installed prefix is outside the checkout at `~/.poracode-smoke/server-current-i38zixfb/prefix`. CLI package qualification also passes against the real tarball (`cli-qualification.log`), not a stub or published registry claim. Parent's strengthened byte verifier compared all **1,680 staged payload files** against archive and installed bytes, with all matches (`installed-byte-verification.json`). Its initial naive pass stopped on macOS-generated AppleDouble records; the final verifier requires the staged counterpart and metadata header and separately records all 1,747 metadata entries rather than counting them as extracted runtime files.

Fresh frozen Electron launch is READY at `~/.poracode-smoke/electron-current-final-20260921/session.json` (mock mode; source hash `1824c8d0d0e1f2f6953cf7b1f63aa51603f7b61f4ced98769b0e1ce516a8be6b`, 4,464 source files). Full deterministic smoke and retained-runtime verification are running. This is development integration, not production performance or external provider/device evidence.

Fresh frozen Electron runtime integrity is verified (artifact SHA256 `7b56a6da1729e01ec4f0bf5c87d3d075e14ac659dfdec85568545cf348851c6f`), but full UI smoke is **FAIL**, not a release pass: Settings MCP connection probes sent no fixture requests and quick-composer submit did not reach the observed main handler. Other scenarios/gates passed; console/runtime error count was zero. Evidence: `electron-current-final-smoke.log` and the session's `full-smoke/smoke-report.json`. Two read-only GLM reviews are investigating these paths. Parent verified that a valid native quick-composer payload reaches the actual main listener and thread-creation intercept, then restored every diagnostic override; the later empty-model schema error is not yet attributed to the original gate.

The first real multi-host harness attempt failed in setup, with seven scenarios unrun: fixture preparation acquired the host root but omitted `initDatabase(runtime.paths.dbPath)`. Parent added explicit DB initialization/close under the fixture lease, corrected teardown evidence to observe actual namespace removal, and started attempt 02. These are scratch-harness corrections, not product fixes; the original failed run remains `electron-current-final-multihost.log`.

### Live multi-host evidence and smoke follow-up — 2026-09-21/22

Actual managed Electron plus independently installed production server qualification passes **7/7** on attempt 07. Evidence: `tmp/v2-production/electron-multihost-qualification/evidence/2026-09-21T23-08-49-880Z-58709/`. Both owners' project/thread order changes, paired restart/reconnect, and paired shutdown isolation were exercised. Grouped/manual sidebar DOM order is asserted with nonempty rows; the final screenshot shows retained paired rows offline. Authenticated host control verifies the installed entrypoint hash. Teardown confirms the owned server exited, its port closed, and its temporary namespace was removed. Earlier attempts' setup/selector assumptions remain recorded as failures. Provider execution is not covered by these inert catalog fixtures. Four harness-owned paired records were subsequently removed through normal actions; no unrelated connection was removed.

A live visit to Remote Environments with stored paired rows exposed React #185. Source mapping identified an allocating Zustand selector in `RemoteServerRow`. GLM's minimal stable-array subscription fix is verified by three real-store regressions (all three fail before, all pass after), 146 neighboring tests, scoped checks and whole typecheck; parent inspected source/tests and verified reported hashes. The compact view has the same selector and is being corrected separately. Frozen Electron does not yet include these fixes.

Quick-composer correction prevents normal thread submission with an unresolved model and surfaces rejected native submissions through the existing error toast. Parent found and corrected an overbroad disabled-state condition: already-configured experiment candidates must remain runnable when the draft model catalog becomes empty. A real editor regression fails only the empty-catalog case before that correction; the final composer/overlay group passes **69/69**. No new user strings or compatibility boundary changes are introduced.

The first traced repeat did not reproduce the earlier MCP probe failure; it reached the original import assertion. Actual owner settings and renderer projection both contained the imported server. Manual reproduction showed the imported row alongside a closing modal retained with `data-exiting=true`. The document was hidden; CDP `Page.bringToFront` did not unhide it. The app's normal `focusWindow()` made the document visible and completed all four retained exits immediately. The smoke runner now requires a visible main window before UI scenarios. Quick-composer input also replaces prior fixture draft content before typing, rather than appending it. No MCP product change was made for this harness failure.

With those harness corrections, `visible-smoke/smoke-report.json` passes Settings (including real owner-backed import persistence), Browser and the native quick-composer mock handoff, with zero console/runtime errors. The full run still **fails** live voice: the synthetic thread is removed by bounded catalog membership during draft-permission navigation. Its captured transition stack identifies `removeThreadRows`; analysis must distinguish an unpersisted mock fixture from a product optimistic-row defect. This remains open, not a full smoke PASS.

A concurrent unrelated commit moved HEAD to `324bf48e385b18f0e589aa7f5cfedbc29e7a02d2` (`fix(opencode): refresh model cache before discovery`). The original staged binary diff remains SHA256 `2924346d9880ff50568de7ca376c62647efb62e95981786782270b915e6922bb`. During checks, 103 checkout-local dependency symlinks were found pointing at `/Library/pnpm/store` instead of the existing same-version/hash paths under `/Users/svecherenko/Library/pnpm/store`. Parent recorded every old link in `dependency-link-drift.json` and repaired only those links; no install, dependency version or lockfile change was made. Scoped composer and harness lint then passed. Cause of the external link drift is not established.

No coordinator architecture commit or push has occurred. New source changes require a fresh artifact checkpoint. Current-artifact experiment/native/web journeys, quiet capacity/soak qualification, external platform gates, and final independent review remain open.

The compact Remote Environments fix is now reviewed at the worker's recorded hashes. Three real-store mobile regressions fail before and pass after; desktop/mobile neighboring suites pass 20/20. The refreshed whole-tree checks pass: **1,529 test files passed, five skipped; 17,062 tests passed, 99 skipped**, plus typecheck and lint. `current-full-tests-ui-followup.log` retains the complete run.

The voice smoke creates an optimistic renderer row but does not invoke the host create/launch operation, so the server has no durable fixture to return during membership reconciliation. The pending managed-root create-intent map is distinct from the app's protected `pendingThreadLaunches` set; conflating them would hide the fixture defect. Parent changed only the smoke fixture: explicitly suppress its unused launch intent, persist the idle row through the existing host-owned `dbUpsertThread` fixture seam, require authoritative membership before navigation, then await `dbDeleteThread` and require absence before local cleanup. No catalog protection rule or production ownership path was weakened. Independent review and a fresh frozen app run are pending.

The older frozen Electron session was reset and stopped through its owner (`electron-current-final-stop.log`: `stop:ok`). Its runtime evidence remains tied to its earlier source hash. The replacement launch is `electron-ui-followup-20260922`; no fresh runtime PASS is yet claimed.

### Fresh frozen checkpoint — 2026-09-22

New Electron runtime source hash `099ae043857ebe41ceea42fff36e071a2754e1d0b5169f61d689a9cf3bc85257` (HEAD `324bf48e3`), artifact hash `ec1af6714327825a584931b0099918beec9b1484391d0d3218ba997253040fb6`; retained runtime verification passes. `electron-ui-followup-20260922/owner-cleanup-smoke/smoke-report.json` is a **full deterministic mock PASS**, exit 0, zero console/runtime errors. This covers Settings including owner-backed MCP import, Browser, voice/draft cancellation and native quick-composer handoff with provider launch intercepted. It does not acknowledge real provider/device/manual gates.

The first fresh run's voice interactions succeeded, but cleanup exposed a stale legacy mapping: `dbDeleteThread` is main-local yet the remote generic procedure allowlist sends it to the supervisor, returning an internal error. Parent used the existing typed host thread-delete command and verified the leftover fixture absent; the smoke now uses that command and its membership assertion. No product catalog rule was changed. A separate ownership audit is checking other generic procedure entries. The worker's alternative fixture-pin proposal was not adopted.

Production server/web build 03 is READY with source hash `c462c34f8e7adeeaffccc2f2ea0f265710bb093d8e3849a2e1ecf8ec85375672`, tarball hash `c29e13a6a7d40662bcba10b8278db84e3ea47a49053cb175ce5ff637d405f158`. Outside-checkout install, pairing/terminal/upgrade/shutdown and real-tarball CLI qualification pass. All **1,682 staged payload files** match the archive and installed release bytes. Prefix: `~/.poracode-smoke/server-ui-followup-15s9sxc2/prefix`.

Current Electron plus that exact installed server again passes **7/7 multi-host scenarios** (`electron-ui-followup-multihost.log`), including nonempty sidebar DOM order and paired shutdown isolation. After the harness stopped its server, the desktop Remote Environments page renders the retained offline paired row without the former React #185 crash; its screenshot was inspected. A 390px viewport check is explicitly **narrow desktop**, not mobile coverage: native Electron keeps the desktop layout even at that width. The compact selector has real-store unit proof; actual web/mobile qualification remains separate.

Follow-up ownership audit has a second live failure: `window.poracode.dbGetLatestThreadGoalItem` for a resident managed thread returns `internal_error`, HTTP 500 (`legacy-goal-read-live.json`). The normal managed hydration path still calls it and treats rejection as hydration failure. Both legacy and bounded host history builders already include the latest goal outside the tail window, so the needed correction should reuse that existing data and remove redundant work rather than invent another goal endpoint. No claim is made that the visible goal itself is necessarily lost; the confirmed defects are the wrong dispatch and failed hydration result. Broader procedure ownership and uncertain-launch retention checks remain in progress.

The managed-root history correction is now implemented narrowly: hydrate from the existing bounded response, which already includes the latest goal, and remove the duplicate legacy read and its false-failure dependency. Absence/error classification and cursor/turn/context handling remain intact; no wire field, version, endpoint or user-facing string changed. A new real HTTP + SQLite regression seeds a goal before 699 newer items, refuses supervisor DB reads, and requires successful hydration with both old goal and tail present. It fails before the fix with `runtimeHydrationStatus: failed`. The final focused group passes **56/56 across three suites** (`managed-history-goal-fix/pass-after-03.log`). The test has its own small file rather than extending the large corrections suite. An intermediate test reused `t-1` across cases and exposed fixture cache retention; switching to a unique fixture ID removed that test-only collision. The earlier full app/artifact evidence remains pinned to the pre-correction checkpoint; fresh runtime verification of this last source change is still pending.

### User-requested plan and goal refinement — 2026-09-22

Replaced the stale proposed-plan status with an active acceptance contract and current evidence assessment. The original audit remains labeled historical, so implemented interests/relay removal/standalone packaging are no longer presented as missing work. The refined goal preserves the intended architecture and authorized commit/push, prioritizes procedure ownership and uncertain-launch correctness, then freezes one candidate for client/platform/load qualification and final delivery. It explicitly rejects rewrites, duplicate endpoints/retry stores, blind file splitting and unsupported performance claims. Overall estimate is about 70% (65–75% range), using implementation 70% weight × 85%, qualification 25% weight × 35%, delivery 5% weight × 0%; it is an estimate of achieved scope, not time-to-finish. The active goal remains active and references this updated plan; no completion or pause was recorded.

The parent ring-accounting counterexample now passes against current source. Frozen raw evidence has 172 records retained before the phase and 453 delivered during it; the segmented union also contains 453, so unread in-phase loss is zero. The earlier report's 625-delivery/172-loss interpretation added the closing ring and dropped counters without subtracting the opening population. Current `trustedInputPopulation` uses push-count deltas and ordinal identities, and its focused native harness suites pass. This corrects population accounting only: the recorded host was still contaminated, so its latency values remain ineligible for a capacity claim and the quiet matrix/soak gates stay open.

### t3code comparison and focused plan amendment — 2026-09-22 UTC

Read the latest 20 messages of thread `989aec99-7d92-4a35-9ce4-7735a0b7d835`, then compared the current dirty implementation and production plan with t3code main at `76cc9b08f19d89012f16d18f47478c4f7b9b0a6f`. Inspected pinned server/client runtime sources, the latest 60 commits, and merged Git burst-control PR #11405. This was source analysis only; no product benchmark or new runtime qualification was performed.

The plan now records the comparison in §0.6 and adds B7: shared Git subprocess admission at the execution owner. Poracode already deduplicates identical Git refreshes, but distinct interests and the parallel reads inside status still reach `execFile` without a shared Git cap. t3code supplies a useful bounded-concurrency example; its eight-permit constant and long-timeout bypass are not adopted as Poracode policy. B7 requires measured limits, bounded waiting, cancellation, separate long-operation policy and actual Git-load/control-latency evidence, including the WSL batch path.

Existing A1/A4/B1/B3/B4/D4 gates were sharpened for empty continuity-frame/serialization cost, queued plus in-flight reservations, tiny-delta write/publication amplification, connection-versus-data freshness, cursor/cache cancellation and persisted N−1 payloads. Poracode already has append-only stream storage, bounded queues, retained outbound reservations and reference-counted interests; these are preserved. Scoped replay is conditional on a measured failure. No Effect RPC/event-sourcing/mobile-framework rewrite or competing cache/connection layer is proposed.

Only the plan and this log were edited. R1/R2 and the active implementation thread retain their existing verification owners; their in-flight source edits are not certified here. The approximate 70% completion estimate remains unchanged. Reviewed local source hashes and pre-edit document copies are under `tmp/t3code-source-review/`; the upstream checkout is under `tmp/t3code-comparison-20260921/`. No commit or push was performed by this comparison task.

### R1/R2/B7 closure and compact-web qualification — 2026-09-22 UTC

R2 uncertain managed-root launch retention is implemented and independently accepted. Only replay-bearing uncertain episodes enter the catalog protection set; fresh markers remain collectable. Real-server regressions prove the exact command id and body survive navigation and repeated absent-membership passes, authoritative retry completion releases them, successful explicit deletion releases them only after command success, ambiguous deletion retains them, and host-originated authoritative deletion cannot leak the marker. The final focused action group passes 50/50; the managed-root directory passed 63/63. No persisted or wire boundary changed.

R1 generic remote procedure ownership is implemented and independently reviewed. The passthrough allowlist is statically constrained to supervisor procedures and has a fail-closed runtime transport check. Nine main-local raw DB/file-manager procedures are classified local/unsupported remotely; the three needed goal/turn/context reads use the existing bounded history contract. Completed turns page to exhaustion with a finite 2000-turn refusal instead of returning a partial array; the experiment response judge also pages to exhaustion and refuses beyond its bounded transcript budget. Real HTTP ownership tests reproduce the former 500s before the change and prove typed refusal/retained dispatch after it. Generated TypeScript/Swift/Kotlin mirrors, parity ledgers and the protocol-12 disposition were regenerated.

Production compact-web qualification against the installed build03 server is PASS: 22/22 visible steps in headless Chrome 153 at desktop and true 390x844 compact layout. Pairing, saved connection row, More → Connections, authoritative Online marker, away/back, reload, verified stop → Offline, restart → Online, and the Projects sheet all passed with zero console warnings/errors and zero React #185 signatures. Final run: `tmp/v2-production/glm-production-mobile-web/run-2026-09-22T01-41-00/`; teardown verified every owned server/browser process exited and ports closed. This remains web-only build03 evidence, not final-byte/native/provider/performance proof.

The t3code comparison's B7 finding was confirmed by fail-before evidence: 12 WSL Git calls and a 21-command batch could start concurrently. The first implementation introduced short/long owner-scoped admission, FIFO bounded waiting, typed refusal/cancellation, exact release and tests, but independent review rejected it because a full WSL worktree refresh has five commands per worktree: two worktrees exceeded the eight-unit cap and the renderer silently swallowed the rejection. The coordinator corrected the design to split arbitrarily large WSL batches into same-class chunks at the live class limit while preserving positional results, propagate typed pressure through every Git status/diff fallback, enforce the queue limit across both classes, preserve empty batches, fence stale test tickets, repump after cancelled/timed-out heads, and expose optional Git active/queued/refusal counters through the existing resource-admission diagnostic. A follow-up review accepted the corrected implementation. Focused diagnostics/admission checks pass 62/62, neighboring Git checks pass 282 with two skipped, and typecheck/lint/format pass. The diagnostic field is additive/optional, so old readers ignore it and new readers accept absence; the existing hop/protocol versions remain unchanged and the generated contract was refreshed.

The first reconciled full run had exactly one stale native-generator expectation after the additive diagnostic introduced two structural types: 1534 files and 17098 tests passed; the one pin failed with the new authoritative source hash/count. After updating only those expected generated values, the targeted generator suite passed 13/13. The full rerun is green: **1535 test files passed, five skipped; 17099 tests passed, 99 skipped** (`tmp/v2-production/final-candidate-checkpoint/full-test-rerun.log`). Current protocol drift check, whole typecheck, whole lint and localization extraction also pass; all 12 non-English catalogs report zero missing.

The plan estimate is now about 75% (70–80% range): implementation 90%, qualification 42%, final commit/push 0%. The final source candidate still needs build04 install/CLI/runtime qualification, final Electron/web reruns, quiet Git/load and soak measurements, native/device and Windows/WSL gates, published N-1 evidence, reconciliation into reviewable commits and verified push.

### Final candidate qualification and commit reconciliation — 2026-09-22 UTC

The first final Electron smoke found a real shared-settings persistence race: concurrent whole-document writes could complete out of order, so an older provider/plugin state overwrote a newer one. The renderer store now serializes writes, coalesces pending state to the latest document and exposes a barrier that drains across restarted write cycles. Its focused suite passes 31/31. A fresh frozen full Electron mock run then passes every scenario with zero console/runtime errors: `~/.poracode-smoke/automated-1790046522224-54994/artifacts/smoke-report.json`. This is deterministic development evidence; provider execution, performance and physical devices remain outside its claim.

The corrected source candidate passes the full suite: **1,535 files passed, five skipped; 17,100 tests passed, 99 skipped** (`tmp/v2-production/final-candidate-checkpoint/full-test-settings-fix.log`). Whole typecheck and lint pass, remote-v3 generation is clean, and extraction reports zero missing translations in all 12 non-English catalogs.

Production build05 pins source `543cc432cf3bb1ccf5b25e30b3740c6b7181283f9c6a67c5573f02767586ca1e`, tarball `eaa97bef4eac26c609cda003699256a842a08857d96e6def59060219a160c475`, web payload `0bb18f7fb285bfd5c5364edd2ff5c3c605532de51d425887876005ce65d8dc7d` and web build version `676f1dfc434b`. Out-of-checkout install and real-tarball CLI qualification pass. Byte verification matches all 1,680 shipped files and accounts for 1,747 AppleDouble metadata records separately. Build05 compact-web qualification passes all 22 desktop/390×844 steps with zero console or React errors; teardown proves every owned process exited and both ports closed (`tmp/v2-production/glm-production-mobile-web/run-2026-09-22T03-21-37/report.md`).

The architecture delta is split into reviewable commits: host extraction (`7e9cf2a28`), bounded runtime/control plane (`ecbd7b36f`), desktop managed-host routing (`5032a427e`), standalone packaging/promotion (`76849b67f`), iOS adoption (`10d3862e7`), Android adoption (`ffb1f2855`) and generated parity/qualification gates (`80eae5304`). Commit hooks passed typecheck; source-format hooks passed for applicable files. Remote delivery is recorded only after the branch push is independently verified.

Remaining mandatory production gates are the quiet reference performance/Git-burst cell with input-latency budgets, 30-minute sustained and overnight soak runs, build05 multi-host rerun, Windows 11 plus WSL2 NAT/mirrored qualification, physical iPhone and lower-tier Android runs, current real-provider journeys, and a real supported published N−1 persisted-payload upgrade. These prevent a production-ready declaration even though the implementation and macOS/web candidate are substantially complete.

### Verified branch delivery — 2026-09-22 UTC

The clean `poracode/v2` branch was pushed normally, without force or history rewriting. The first delivered head matched locally and remotely at `3811d24852fa060d48a4d77caea8335890b37b53`. The configured origin reported that the repository has moved to `git@github.com:Porabuild/Poracode.git`, but accepted the push through the existing URL; no remote configuration was changed during close-out. This final documentation commit records that verification and advances the delivery component from 0% to 100%.

The resulting overall estimate is **about 80%**: implementation 90% at 70% weight, production qualification 48% at 25% weight, and delivery 100% at 5% weight. The open platform, soak, performance, provider and published N−1 gates remain mandatory and keep the goal active.
