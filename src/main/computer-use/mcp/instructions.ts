export const COMPUTER_USE_MCP_INSTRUCTIONS =
  'Use the computer_use MCP server to inspect and control native Windows, macOS, or Linux apps on the host desktop. Start with computer_use.api or list_apps, select a returned window, then call get_window_state with include_text:true. list_apps without a query reports apps that currently have targetable windows; pass query to search installed apps, then give a returned id to launch_app. Prefer find_elements plus invoke_element or set_element_value for accurate semantic actions; use screenshot coordinates only when the app exposes no suitable element. Call computer_use.enable once immediately before the first control action and keep it enabled across the uninterrupted workflow; background actions show a small badge without taking the user\'s focus, mouse, or keyboard. click, drag, scroll, type_text, and press_key use mode:"background" by default and never silently escalate to foreground. Read every action\'s delivery or refused object. Use mode:"foreground" only when the user asked for takeover or a refusal explicitly recommends it, and tell the user before taking over; activate_window and launch_app are foreground operations. On native Wayland, coordinate input may use the consented desktop portal and reports delivered:"foreground" with wayland_portal_fallback. Verify effects with get_window_state, then call computer_use.disable before pausing for user input, waiting, or finishing. Coordinates are relative to the top-left of the full window frame and must come from the latest screenshot; divide screenshot coordinates by its reported scale. Refresh stale windows or element snapshots when instructed. Prefer the browser MCP server for web pages. Locked desktops, secure prompts, permission prompts, and password/authentication surfaces require the user.';
