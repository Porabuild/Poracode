import { uniqueCoreSkillForBuiltInMcp } from "@/shared/plugins/builtInCoreSkills";

/**
 * The Computer Use plugin's core skill, the one `@Computer Use` mentions.
 * Read from `plugin.json` so a rename there cannot leave initialize/api
 * pointing at a skill that no longer exists.
 */
export const COMPUTER_USE_CORE_SKILL = uniqueCoreSkillForBuiltInMcp("computer-use");

// Ordering is deliberate: the background-only rule comes before any mention of
// foreground mode. An agent that reads "retry in foreground" early treats
// takeover as the sanctioned way out of a refusal, and every takeover seizes
// the user's real mouse, keyboard, and window focus.
export const COMPUTER_USE_MCP_INSTRUCTIONS =
  `Use the computer_use MCP server to inspect and control native Windows, macOS, or Linux apps. Before the first control action, load the ${COMPUTER_USE_CORE_SKILL} skill by name — it is this plugin's core skill and the operating procedure. These instructions are the contract for every tool result. Work in the background: ` +
  'a badge appears, and the user\'s pointer, keyboard, and focused app stay theirs. The one disturbance is window order inside the app you are driving — see type_text and press_key. Background is the only mode you may choose on your own. mode:"foreground" and activate_window are takeovers; use them only when the user asked for a takeover, and tell the user before you do. Never activate or raise a window to make an action land, and never alternate between background and foreground while working: element actions reach a window that is not frontmost, not focused, and even minimized. Start with list_apps or list_windows, then get_window_state with include_text:true and include_screenshot:false for semantic tasks; request a screenshot for visual checks or coordinates; call api only when capability or permission details are needed. list_apps without a query lists apps that have targetable windows; pass query to search installed apps, then give a returned id to launch_app. Element ids in a get_window_state tree are actionable; pass accessibility.snapshotId as find_elements.snapshot_id to filter that tree. A find_elements result returns snapshotId for reuse. The tree omits some actions to stay small — scroll, context_menu and set_value on every node, click on every node, and invoke on button, link and menuitem — so a line with no actions= is not inert; find_elements reports each node\'s advertised list, and on macOS that list includes scroll because invoke walks ancestors. When a webarea sits beside browser chrome the text starts at the page and omits that chrome; consecutive clipped siblings collapse to a count. Use find_elements by name for a chrome control or a hidden row — their ids stay in the snapshot. On macOS, invoke_element action "scroll" on the node you want revealed: the host walks up to whatever can actually scroll and notes scrolled_ancestor when it was not the node you named. Scrolling the container itself does nothing and reports verified:"unchanged". On Windows and Linux, use a node whose find_elements actions list includes scroll. Prefer invoke_element or set_element_value; use screenshot coordinates only when no element can do the job. Call computer_use.enable immediately before the first control action and keep it enabled across the uninterrupted workflow. click, drag, scroll, type_text, and press_key use mode:"background" by default and never silently escalate. Use perform for a short deterministic sequence of element/value/key/text background actions and one final observation; it stops on refusal, error, or unexpected foreground delivery. After a partial batch failure, inspect completed steps and current state before continuing; do not replay the whole batch. Prefer observe:"text", or "screenshot"/"both" for visual checks. Reuse observation.state. Keep using the window you hold until a result hands you a new one. Read every action\'s delivery or refused object. A browser or Electron window ignores raw background mouse events. The host resolves a background click against the accessibility tree and presses the element under the point (delivery.target, note coordinate_resolved_by_tree). It refuses when nothing pressable sits there and always for a right, double, or middle click. Recover with find_elements plus invoke_element or set_element_value, scroll with invoke_element action "scroll", or use the browser MCP server. That refusal is not a reason to take the window over. An accessibility tree is the app\'s own asynchronous report: after a background element action a browser can keep answering with the pre-action state for up to half a second. A read-back that shows no change is not failure — re-read once, and never escalate on it. type_text and press_key raise an unfocused window inside its app (note in_app_focus_changed); prefer set_element_value, and never ping-pong keys between two windows of the same app. A background_unavailable refusal means use a semantic action; follow the hint before considering anything that takes focus. Read delivery.verified as what the host could observe, not as success or failure. "confirmed" means it watched the effect. "unchanged" means it watched and nothing moved: pick a different element rather than repeating or escalating. "unverified" means accepted with nothing local to check, which is still normal for a button that does not publish state; if a bundled observation already shows the effect, treat that as the check and do not retry, because a second invoke would fire it twice. Toggle a checkbox or radio with invoke_element action "toggle" — set_element_value is for text. A node marked clipped has no area: invoke_element action "scroll" to reveal it; do not click it. If no background route can do the job, stop and ask the user for a takeover instead of taking one. While the screen is locked, foreground input, activate_window, and foreground launches are refused with screen_locked. On a locked macOS desktop stop and ask the user to unlock: there is no window content to observe. When keep-awake is on, enable reports keepAwake:true; always end with computer_use.disable. Verify, then disable before pausing, waiting, or finishing. Coordinates are window-frame relative from the latest screenshot; divide by the screenshot\'s scale. Refresh stale windows or snapshots when instructed. Prefer the browser MCP server for web pages. Locked desktops, secure prompts, permission prompts, and password surfaces require the user.';
