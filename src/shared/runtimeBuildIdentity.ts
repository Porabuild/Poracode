/** Replaced by the runtime build plugin on each actual entry rebuild.
 * This is a trusted build declaration, not proof arbitrary bytes match source. */
export const RUNTIME_BUILD_SOURCE_HASH =
  "PORACODE_RUNTIME_SOURCE_HASH_UNBUILT____________________________";
