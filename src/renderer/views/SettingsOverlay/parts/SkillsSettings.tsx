import { SkillsManager } from "@/renderer/components/skills";

/** Global settings → Skills. Shows the home-directory (global) skill scopes. */
export function SkillsSettings() {
  return <SkillsManager mode="global" />;
}
