/**
 * Placeholder shown in a credential field whose value is already stored. The
 * real secret never reaches the renderer, so agent settings render this mask
 * instead — keep every credential field using the same one.
 */
export const SAVED_CREDENTIAL_MASK = "***********";
